# 登录认证链路分析报告

## 核心结论

**后端完全没有实现任何认证/鉴权机制。** 所谓"登录状态"完全依赖浏览器 `localStorage` 中一个普通 JSON 对象维持，前端路由守卫只检查该 key 是否存在，而后端所有业务接口（商品、分类、供应商、订单）对任何人都是完全公开的，不校验任何凭证。

---

## 一、登录成功那一刻发生了什么

### 前端侧

登录逻辑位于 [Login.vue](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/frontend/src/views/Login.vue#L82-L94)：

```javascript
const handleLogin = async () => {
  loading.value = true;
  try {
    const data = await request.post('/auth/login', loginForm);
    localStorage.setItem('user', JSON.stringify(data));
    message.success('登录成功');
    router.push('/products');
  } catch (error) {
    // Error handled by interceptor
  } finally {
    loading.value = false;
  }
};
```

流程：
1. 向 `POST /api/auth/login` 发送 `{username, password}`
2. 后端返回 `{id, username, role}`（注意：**没有 token、没有 session、没有 cookie**）
3. 前端把这个对象原样 `JSON.stringify` 后存入 `localStorage`，key 为 `"user"`
4. 跳转到 `/products`

### 后端侧

登录接口位于 [AuthController.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/backend/src/main/java/com/supermarket/controller/AuthController.java#L35-L51)：

```java
@PostMapping("/login")
public ResponseEntity<?> login(@RequestBody User loginUser) {
    User user = userRepository.findByUsername(loginUser.getUsername());

    if (user == null || !user.getPassword().equals(loginUser.getPassword())) {
        Map<String, String> error = new HashMap<>();
        error.put("message", "用户名或密码错误");
        return ResponseEntity.badRequest().body(error);
    }

    Map<String, Object> response = new HashMap<>();
    response.put("id", user.getId());
    response.put("username", user.getUsername());
    response.put("role", user.getRole());

    return ResponseEntity.ok(response);
}
```

关键点：
- 仅校验用户名和密码是否匹配（密码明文存储、明文比对）
- 响应中只有用户基本信息，**不签发 JWT、不创建 HttpSession、不 Set-Cookie**
- 登录成功后，服务端不保留任何"已登录"的状态

---

## 二、登录状态如何"维持"

### 2.1 页面路由拦截（前端唯一关卡）

路由守卫位于 [router/index.js](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/frontend/src/router/index.js#L42-L52)：

```javascript
router.beforeEach((to, from, next) => {
    const user = localStorage.getItem('user');
    if (to.meta.requiresAuth && !user) {
        next('/login');
    } else if (to.path === '/login' && user) {
        next('/products');
    } else {
        next();
    }
});
```

判断逻辑极其简单：
- 只要 `localStorage.getItem('user')` 返回**非 null 值**，就认为"已登录"
- 不检查值的内容、不验证签名、不检查过期时间
- 受保护路由标记了 `meta: { requiresAuth: true }`：`/products`、`/categories`、`/suppliers`

### 2.2 每次拉数据时——请求里带了什么？

HTTP 客户端封装位于 [request.js](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/frontend/src/api/request.js)：

```javascript
const instance = axios.create({
    baseURL: '/api',
    timeout: 10000
});

instance.interceptors.response.use(
    response => response.data,
    error => {
        const msg = error.response?.data?.message || '网络请求失败';
        message.error(msg);
        return Promise.reject(error);
    }
);
```

**重要发现：没有请求拦截器。**

- 没有 `instance.interceptors.request.use(...)`
- 没有在请求头中附加 `Authorization: Bearer xxx`
- 没有发送任何 cookie（默认 axios 也不发，且后端也没 Set-Cookie）
- 每次调用 `/api/products`、`/api/categories` 等接口时，请求里**不携带任何认证凭证**

### 2.3 页面顶部显示用户名

位于 [App.vue](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/frontend/src/App.vue#L76-L82)：

```javascript
const username = computed(() => {
  const user = localStorage.getItem('user');
  if (user) {
    return JSON.parse(user).username;
  }
  return '';
});
```

同样是直接读 localStorage，没有任何验证。

### 2.4 退出登录

位于 [App.vue](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/frontend/src/App.vue#L84-L87)：

```javascript
const handleLogout = () => {
  localStorage.removeItem('user');
  router.push('/login');
};
```

只是删了本地存储里的 key，没有调用任何后端"登出"接口（后端也没有）。

---

## 三、后端业务接口到底认不认这个状态？

### 结论：完全不认。

逐项核查：

| 控制器 | 路径前缀 | 是否有认证检查 | 证据 |
|--------|----------|----------------|------|
| [ProductController](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/backend/src/main/java/com/supermarket/controller/ProductController.java) | `/api/products` | ❌ 无 | 无 Filter/Interceptor/Security 注解 |
| [CategoryController](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/backend/src/main/java/com/supermarket/controller/CategoryController.java) | `/api/categories` | ❌ 无 | 同上 |
| [SupplierController](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/backend/src/main/java/com/supermarket/controller/SupplierController.java) | `/api/suppliers` | ❌ 无 | 同上 |
| [PurchaseOrderController](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/backend/src/main/java/com/supermarket/controller/PurchaseOrderController.java) | `/api/orders` | ❌ 无 | 同上 |
| [AuthController](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/backend/src/main/java/com/supermarket/controller/AuthController.java) | `/api/auth` | 登录/注册本身公开 | 仅校验密码，登录后不发凭证 |

### 后端项目层面的证据

1. **pom.xml 没有 Spring Security 依赖**（[pom.xml](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/backend/pom.xml#L19-L48)）：只有 `spring-boot-starter-data-jpa`、`spring-boot-starter-web`、`spring-boot-starter-validation`、MySQL 驱动、Lombok。

2. **没有 Filter / Interceptor / SecurityConfig**：对整个 backend 目录全局搜索 `Filter|Interceptor|Security|@PreAuthorize|@Secured|HttpSession|Cookie|JWT|Token`，**零匹配**。config 包下只有一个 [DataInitializer.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/backend/src/main/java/com/supermarket/config/DataInitializer.java)（用于初始化演示数据）。

3. **所有控制器都加了 `@CrossOrigin(origins = "*")`**：允许任意域名的网页直接跨域调用这些接口，连 CSRF 防护的意义都不存在（因为根本没有会话可冒用）。

4. **应用主类 [SupermarketApplication.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/backend/src/main/java/com/supermarket/SupermarketApplication.java) 无任何额外配置**。

### 架构图（实际状态）

```
浏览器                                    后端 Spring Boot
┌─────────────────────┐                 ┌──────────────────────────┐
│ localStorage.user   │                 │                          │
│ (仅前端自己看)       │                 │  /api/auth/login         │
│         │           │  POST /login    │  校验密码 → 返回用户信息   │
│         ▼           │ ──────────────► │  (不发 token/cookie)      │
│ 路由守卫检查 key 存在 │                 │                          │
│         │           │                 │  /api/products  ← 公开   │
│         ▼           │  GET /products  │  /api/categories ← 公开  │
│ 页面组件发请求       │ ──────────────► │  /api/suppliers  ← 公开  │
│ (不带任何凭证)       │ ◄────────────── │  /api/orders     ← 公开  │
└─────────────────────┘   直接返回数据    │  (无任何认证检查)         │
                                          └──────────────────────────┘
```

---

## 四、什么情况下会绕过"登录"出问题

由于后端完全无鉴权，以下场景均可直接绕过，按严重程度排列：

### 4.1 直接调用 API（最严重）

任何人不需要登录、不需要浏览器，直接用 curl、Postman、脚本就能增删改查所有数据：

```bash
# 拉取所有商品（无需登录）
curl http://localhost:8080/api/products

# 新增商品（无需登录）
curl -X POST http://localhost:8080/api/products \
  -H "Content-Type: application/json" \
  -d '{"name":"黑客商品","price":0.01,"stock":99999,"category":{"id":1}}'

# 删除商品（无需登录）
curl -X DELETE http://localhost:8080/api/products/1
```

分类、供应商、订单接口同理。前端的"必须登录"提示对直接打 API 的人完全不存在。

### 4.2 浏览器控制台一行代码"登录"

在浏览器 DevTools Console 中执行：

```javascript
localStorage.setItem('user', JSON.stringify({id: 1, username: 'hacker', role: 'ADMIN'}));
location.reload();
```

路由守卫检测到 `user` key 存在，直接放行进入所有页面，且顶部用户名显示为 `hacker`。由于后端不校验，所有数据操作都能正常成功。

### 4.3 手动改 localStorage 提权

登录返回的 role 字段前端没有任何地方使用（没有角色权限控制、没有按钮级权限），但即使未来加了，由于数据存在本地，用户可以随意把自己 role 改成 `ADMIN`。

### 4.4 跨站调用

所有 Controller 都配置了 `@CrossOrigin(origins = "*")`，意味着任意恶意网站都可以在其页面中通过 JavaScript 调用本系统的 API（用户的浏览器会直接发请求，且由于没有 cookie/凭证机制，实际上连"冒用身份"都谈不上——接口本身就是公开的）。

### 4.5 刷新页面后的表现

- 正常用户刷新：`localStorage` 持久化，user 还在，路由守卫放行，页面正常加载数据。
- 换一个浏览器/隐身窗口：没有 localStorage，路由守卫跳登录页。但后端数据仍然可以被直接访问。

### 4.6 注册接口也无防护

`POST /api/auth/register` 完全公开，任何人可以随意注册账号（虽然注册了也没什么用，因为后端不区分用户）。

---

## 五、其他附带问题

1. **密码明文存储**：[DataInitializer.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/backend/src/main/java/com/supermarket/config/DataInitializer.java#L36-L48) 中 admin/123456、user/123456 明文写入数据库；[AuthController.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/backend/src/main/java/com/supermarket/controller/AuthController.java#L39) 用 `equals()` 直接比对明文。代码注释也写了"生产环境应使用BCrypt加密"。

2. **role 字段形同虚设**：User 实体有 `role`（USER/ADMIN），登录也返回了，但前端没有任何角色判断，后端也没有方法级安全控制，等于摆设。

3. **无会话过期/续期机制**：localStorage 永不过期，除非用户主动点退出或清浏览器数据；但反过来，由于后端无状态，"踢出登录"也无法实现。

4. **生产环境 nginx 配置也无鉴权**：[nginx.conf](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/frontend/nginx.conf) 只做了静态资源服务和 `/api` 反向代理，没有任何 access control。

---

## 六、总结

| 环节 | 实际情况 |
|------|----------|
| 登录成功后后端发什么凭证 | **什么都不发**，只返回用户基本信息 JSON |
| 前端如何记住登录态 | `localStorage.user` 存了那个 JSON |
| 路由守卫判断依据 | 只判断 `localStorage.getItem('user')` 是否非 null |
| 后续 API 请求带不带凭证 | **不带**，没有 token、没有 cookie、没有 Authorization 头 |
| 后端商品/分类/供应商/订单接口是否鉴权 | **完全不鉴权**，所有接口公开访问 |
| 后端有没有 Spring Security / Filter / Interceptor | **都没有**，pom 里连依赖都没引 |
| 直接 curl 后端接口能不能拿到数据 | **能**，无需任何认证 |
| 浏览器控制台能不能伪造登录 | **能**，写一行 localStorage 即可 |

一句话概括：**这是一个"前端假装做了登录拦截，后端完全裸奔"的实现。** 登录状态只存在于用户自己的浏览器里，后端对每个请求都一视同仁，没有任何方式验证调用者是谁、是否登录过。

---

## 七、后端接口完整清单（逐个核查）

核查范围：[AuthController](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/backend/src/main/java/com/supermarket/controller/AuthController.java)、[ProductController](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/backend/src/main/java/com/supermarket/controller/ProductController.java)、[CategoryController](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/backend/src/main/java/com/supermarket/controller/CategoryController.java)、[SupplierController](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/backend/src/main/java/com/supermarket/controller/SupplierController.java)、[PurchaseOrderController](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/backend/src/main/java/com/supermarket/controller/PurchaseOrderController.java)。

所有接口统一情况：
- 类上都有 `@CrossOrigin(origins = "*")`，任意网页可跨域调用。
- 没有任何方法/类级别的 `@PreAuthorize`、`@Secured`、Filter、Interceptor。
- 方法参数里没有 `HttpSession`、`@RequestHeader("Authorization")`、`Principal` 等身份对象。
- 结论：**下表每一行"身份校验"都是"无"，"匿名直调"都是"能通"。**

### 7.1 认证接口 `/api/auth`

| # | 方法 | 路径 | 身份校验 | 匿名直调 | 通了能干啥 |
|---|------|------|----------|----------|------------|
| 1 | POST | `/api/auth/register` | 无 | 能 | 新增用户账号。请求体绑定到 [User](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/backend/src/main/java/com/supermarket/model/User.java) 后直接 `save`。**注意：`role` 字段会被一并绑定保存**，因此匿名请求体里带 `"role":"ADMIN"` 就能直接注册管理员（见 [AuthController.java#L20-L33](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/backend/src/main/java/com/supermarket/controller/AuthController.java#L20-L33)）。返回值里密码被置 null，不会回传。用户名重复时返回 400。 |
| 2 | POST | `/api/auth/login` | 无（登录本身就是公开入口） | 能 | 按用户名查库，用 `equals()` 明文比对密码。成功返回 `{id, username, role}`，**不签发任何凭证**。失败返回 400 + "用户名或密码错误"。 |

### 7.2 商品接口 `/api/products`

数据结构见 [Product.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/backend/src/main/java/com/supermarket/model/Product.java)：id、name、barcode、price、stock、category（关联分类对象）。

| # | 方法 | 路径 | 身份校验 | 匿名直调 | 通了能干啥 |
|---|------|------|----------|----------|------------|
| 3 | GET | `/api/products` | 无 | 能 | 不带 `name` 参数时 `findAll()` 返回全部商品；带 `name` 时按名称模糊匹配（忽略大小写）。能读到所有商品的 id、名称、条形码、价格、库存及其所属分类的 id/名称/描述。无分页。 |
| 4 | GET | `/api/products/category/{categoryId}` | 无 | 能 | 按分类 id 查出该分类下全部商品（[ProductRepository#L10](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/backend/src/main/java/com/supermarket/repository/ProductRepository.java#L10)）。 |
| 5 | POST | `/api/products` | 无 | 能 | 直接 `save` 请求体里的 Product，新增一条商品记录。可任意设置 name、barcode、price、stock、category（传 `{"id":...}` 即可关联）。方法上没有 `@Valid`，price/stock 的 `nullable=false` 只是建表约束，运行时不校验，传负数/空值也可能落库（取决于数据库约束）。 |
| 6 | PUT | `/api/products/{id}` | 无 | 能 | 按 id 查出商品后，覆盖 name、price、stock、barcode、category 五个字段并保存（[ProductController.java#L37-L49](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/backend/src/main/java/com/supermarket/controller/ProductController.java#L37-L49)）。可任意改价、改库存。id 不存在抛 RuntimeException。 |
| 7 | DELETE | `/api/products/{id}` | 无 | 能 | 按 id 物理删除商品。调的是 `deleteById`，id 不存在也不报错（返回 200）。若该商品被采购单引用，会触发数据库外键报错（500），但这不是鉴权拦截。 |

### 7.3 分类接口 `/api/categories`

数据结构见 [Category.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/backend/src/main/java/com/supermarket/model/Category.java)：id、name（唯一）、description。

| # | 方法 | 路径 | 身份校验 | 匿名直调 | 通了能干啥 |
|---|------|------|----------|----------|------------|
| 8 | GET | `/api/categories` | 无 | 能 | `findAll()` 返回全部分类。 |
| 9 | POST | `/api/categories` | 无 | 能 | 新增分类。name 在库里有唯一约束，重名会报 500（数据完整性异常，不是业务校验）。 |
| 10 | DELETE | `/api/categories/{id}` | 无 | 能 | 按 id 删除分类。若仍有商品挂在该分类下，外键约束可能导致 500；无引用时直接删除。 |

### 7.4 供应商接口 `/api/suppliers`

数据结构见 [Supplier.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/backend/src/main/java/com/supermarket/model/Supplier.java)：id、name、contact、phone、address。

| # | 方法 | 路径 | 身份校验 | 匿名直调 | 通了能干啥 |
|---|------|------|----------|----------|------------|
| 11 | GET | `/api/suppliers` | 无 | 能 | `findAll()` 返回全部供应商，含联系人、电话、地址。 |
| 12 | POST | `/api/suppliers` | 无 | 能 | 新增供应商。 |
| 13 | PUT | `/api/suppliers/{id}` | 无 | 能 | 按 id 查出后覆盖 name、contact、phone、address 四个字段（[SupplierController.java#L29-L40](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/backend/src/main/java/com/supermarket/controller/SupplierController.java#L29-L40)）。id 不存在抛 RuntimeException。 |
| 14 | DELETE | `/api/suppliers/{id}` | 无 | 能 | 按 id 删除供应商。被采购单引用时可能外键报错 500。 |

### 7.5 采购单接口 `/api/orders`

数据结构见 [PurchaseOrder.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/backend/src/main/java/com/supermarket/model/PurchaseOrder.java)：id、supplier（对象）、product（对象）、quantity、unitPrice、totalAmount、orderDate、status。

| # | 方法 | 路径 | 身份校验 | 匿名直调 | 通了能干啥 |
|---|------|------|----------|----------|------------|
| 15 | GET | `/api/orders` | 无 | 能 | 不带 `status` 时 `findAll()` 返回全部采购单；带 `status` 时按状态过滤（PENDING/COMPLETED/CANCELLED）。返回结果里**嵌套了完整的 supplier 和 product 对象**（product 里还带 category），一次调用就能把供应商、商品、分类信息全拖走。 |
| 16 | POST | `/api/orders` | 无 | 能 | 新增采购单。可任意指定供应商、商品、数量、单价、总额、状态。orderDate 未在请求体里传时由实体默认值 `LocalDateTime.now()` 填充。 |
| 17 | PUT | `/api/orders/{id}` | 无 | 能 | 按 id 查出后覆盖 supplier、product、quantity、unitPrice、totalAmount、status（[PurchaseOrderController.java#L32-L45](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/backend/src/main/java/com/supermarket/controller/PurchaseOrderController.java#L32-L45)）。可把订单状态任意改成 COMPLETED/CANCELLED。 |
| 18 | DELETE | `/api/orders/{id}` | 无 | 能 | 按 id 删除采购单。 |

### 7.6 清单小结

- 共 **18 个接口**，**0 个**做了身份校验，**18 个**可匿名直调。
- 没有"按当前登录用户过滤数据"的概念——业务表里压根没有 user/owner 外键，所有数据对所有人共享。
- 没有单条查询接口（没有 `GET /api/products/{id}` 之类），读操作全是全表 `findAll`，无分页。
- **额外发现（不影响原结论，但值得单独指出）**：采购单接口在前端根本没有对应页面。[router/index.js](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/frontend/src/router/index.js) 只注册了 products/categories/suppliers，views 目录下也没有订单组件。也就是说 `/api/orders` 这 4 个接口目前是"裸奔且前端没用到"的状态，但后端照样对外暴露。

---

## 八、控制台伪造 user 进入页面的实际推演

推演前提：在登录页（或任意页面）打开 DevTools Console，写入一个伪造的 `localStorage.user`，然后刷新/跳转。

前端读取 `user` 的位置只有三处，全部不校验内容真实性：
1. [router/index.js#L44](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/frontend/src/router/index.js#L44)：只做 `localStorage.getItem('user')`，判断返回值是否为 null，**连 `JSON.parse` 都不做**。
2. [App.vue#L76-L82](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/frontend/src/App.vue#L76-L82)：`JSON.parse(user).username` 取用户名显示。
3. [App.vue#L85](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/frontend/src/App.vue#L85)：退出登录只做 `removeItem`。

**关键：`role` 字段在整个前端没有任何代码读取**（全局搜索 `.role` 零匹配），所以伪造时填不填 role、填什么，对页面没有任何可见影响。

### 场景 A：伪造一个完整对象（最常见）

```javascript
localStorage.setItem('user', JSON.stringify({id: 1, username: 'hacker', role: 'ADMIN'}));
location.reload();
```

逐项表现：

| 环节 | 表现 | 露馅/无感 |
|------|------|-----------|
| 路由守卫 | `getItem('user')` 得到非空字符串 → 放行所有 `requiresAuth` 页面 | 无感 |
| 顶栏用户名 | `JSON.parse(user).username` = `"hacker"` → 图标旁显示 **hacker** | 无感（显示的就是你塞进去的名字） |
| 左侧菜单 | 三个菜单项（商品/分类/供应商）全部正常渲染，与角色无关 | 无感 |
| 商品管理页 | `onMounted` 调 `/api/products`、`/api/categories`，后端无鉴权正常返回 → 表格正常显示数据，增删改查全部可用 | 无感 |
| 分类管理页 | 调 `/api/categories`，正常加载、增删 | 无感 |
| 供应商管理页 | 调 `/api/suppliers`，正常加载、增删改 | 无感 |
| 点"退出登录" | 执行 `localStorage.removeItem('user')` 后跳 `/login`，回到登录页 | 无感，行为和真用户完全一样 |
| 再次访问 /login | 守卫见 `user` 已被删 → 停在登录页 | 无感 |

结论：**整条链路完全无感，和真实登录的用户体验没有任何可感知的差别。** 后端不校验，前端不校验，伪造者可以为所欲为。

### 场景 B：只塞一个空对象 `{}`

```javascript
localStorage.setItem('user', '{}');
```

- 路由守卫：非空字符串 → 放行。
- 顶栏用户名：`JSON.parse('{}').username` = `undefined` → Vue 模板里 `{{ undefined }}` 渲染为**空字符串**，即只有用户图标、图标旁边是空白（没有名字）。
- 其他页面、所有接口、退出登录：全部正常。

露馅点：**顶栏用户名位置是空白的**，真实登录用户这里一定会显示用户名。这是唯一的视觉破绽，但不会报错、不会跳转、不影响任何功能。

### 场景 C：塞非 JSON 的垃圾字符串

```javascript
localStorage.setItem('user', 'hello');
```

- 路由守卫：非空字符串 → 仍然放行（`'hello'` 是 truthy）。
- 顶栏用户名：`App.vue` 的 computed 执行 `JSON.parse('hello')` 抛 `SyntaxError`。这是一个 computed 里的同步异常，**会导致 App.vue 根组件渲染失败**：开发环境下页面会出现 Vue 的错误遮罩/白屏，生产构建下整个布局（侧边栏、顶栏、router-view）都渲染不出来。

露馅点：**页面直接报错/白屏**，能明显看出"坏了"。但注意——这不是被"识别出未登录"，而是前端自己解析崩了；浏览器地址栏仍然停留在受保护路由，不会被踢回登录页。

### 场景 D：塞字符串 `"null"`

```javascript
localStorage.setItem('user', 'null');
```

- 路由守卫：`getItem` 返回字符串 `"null"`，这是个非空字符串、truthy → **放行**。这里有个 JS 语义陷阱：字符串 `"null"` 不是 null。
- 顶栏用户名：`JSON.parse("null")` = `null`，接着访问 `null.username` 抛 `TypeError` → 和场景 C 一样导致 App.vue 渲染崩溃。

露馅点：同场景 C，白屏/报错，但路由不会跳转。

### 场景 E：访问 `/login` 时已带伪造 user

如果伪造 user 存在时手动访问 `/login`：
- 守卫命中 `to.path === '/login' && user` 分支，重定向到 `/products`。
- 行为与真实"已登录再访问登录页"完全一致，无感。

### 8.1 伪造推演小结

- 只要塞的是**结构完整、可正常解析的 JSON**（场景 A），从登录到进入任意页面、拉数据、增删改、退出登录，**全程零露馅**，顶栏会老老实实显示你伪造的 username。
- `role` 字段完全无用，前端不读、后端不校验，伪造时随便填。
- 唯一会"露馅"的两种情况：
  1. 塞了对象但没带 `username` 字段（场景 B）：顶栏用户名为空白，但功能一切正常。
  2. 塞的不是合法 JSON 或是 `"null"`（场景 C/D）：`App.vue` 解析崩溃导致白屏。但这是前端渲染 bug，**不是被认证机制拦截**，且不会跳回登录页。
- 退出登录对伪造者同样有效——它本来就是纯前端操作，删 localStorage 而已。

---

## 九、与原结论的对照说明

本轮深入核查后，**原报告第一至第六节的结论全部成立，未发现矛盾**。具体复核：

- 原结论"后端登录成功不发凭证"——复核确认：[AuthController.java#L45-L50](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/backend/src/main/java/com/supermarket/controller/AuthController.java#L45-L50) 只返回用户信息，无 token/session/cookie。
- 原结论"商品/分类/供应商/订单接口完全不鉴权"——逐接口核查 18 个端点，全部无任何身份校验，结论成立。
- 原结论"控制台塞假 user 可进页面"——推演确认，且完整对象下全程无感。

本轮**新增、但不与原结论冲突**的发现：

1. **注册接口存在越权创建管理员的隐患**：`/api/auth/register` 直接把请求体绑定到 User 并保存，`role` 字段未做屏蔽，匿名调用者可在请求体里带 `"role":"ADMIN"` 创建管理员账号（[AuthController.java#L21-L29](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/backend/src/main/java/com/supermarket/controller/AuthController.java#L21-L29)）。由于后端目前不基于 role 做权限控制，这个隐患暂时无法被实际利用来提权，但属于明确的逻辑缺陷。
2. **采购单接口在前端没有对应页面**：`/api/orders` 的 4 个接口后端完整实现且裸奔，但前端路由和视图里均无入口，属于未被使用却依然对外暴露的攻击面。
3. **伪造 user 的崩溃边界**：原报告只提了"写一行 localStorage 即可"，本轮细化出——只有塞合法 JSON 才完全无感；塞非 JSON 或字符串 `"null"` 会因 `App.vue` 的 `JSON.parse` 异常导致白屏（这是前端健壮性问题，不是认证拦截）。

---

## 十、鉴权整改方案（仅方案，不改代码）

下面给出一套可直接落地的改造方案，落到每个具体文件和接口。核心目标是让"登录状态"真正由后端签发并校验，前端只负责携带凭证，而不是自己说了算。

### 10.1 路线选择：JWT（token）还是 Session

#### 两条路线对比

| 维度 | JWT（无状态 token） | HttpSession（JSESSIONID Cookie） |
|------|---------------------|----------------------------------|
| 服务端状态 | 无，token 自包含用户信息和签名 | 有，会话存在服务端内存/Redis |
| 横向扩展 | 天然支持，多实例无需共享会话 | 单实例可用内存；多实例需 Spring Session + Redis |
| 注销/踢人 | 默认无法即时吊销，需额外黑名单或短有效期+刷新 token | 直接 `invalidate()` 即可，即时生效 |
| 凭证存放 | 前端 `Authorization` 头；放 localStorage 有 XSS 窃取风险，放内存更安全但刷新丢失 | HttpOnly + Secure + SameSite Cookie，JS 读不到，抗 XSS 窃取 |
| CSRF | 用 Header 携带时基本免疫 CSRF | Cookie 自动携带，需开 CSRF 防护（SameSite 可缓解） |
| 与本项目契合度 | 前端是纯 SPA、后端是纯 REST JSON API，天然契合 | 项目无 Redis、单 backend 容器，内存 session 够用但重启即掉线 |
| 实现复杂度 | 需自己写签发/校验 Filter、处理过期刷新 | Spring Security 开箱即用，配置更少 |

#### 本项目的取舍

**推荐 JWT 路线。** 理由：

1. 这是一个"Vue SPA + Spring Boot REST API"架构，所有 Controller 返回 JSON、没有服务端渲染页面，token 模式是最自然的匹配。
2. [docker-compose.yml](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/docker-compose.yml) 里只有单实例 backend、没有 Redis，引入 session 共享反而要加基础设施；而 JWT 无状态，将来加实例也不用改。
3. 角色模型简单（只有 USER/ADMIN），直接把 role 写进 JWT claim，配合方法级注解即可做权限控制，不需要查库。
4. 前端已经有统一的 axios 实例 [request.js](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/frontend/src/api/request.js)，加一个请求拦截器塞 Header 成本极低。

**但必须接受 JWT 的两个代价，并在方案里缓解：**

- **XSS 窃取风险**：第一版为了改动最小、保持"刷新不掉线"的体验，token 仍存 localStorage（和现在存 user 的位置一致）。这是可接受的第一版；第二阶段应把 access token 放内存、refresh token 放 HttpOnly Cookie，降低窃取风险。
- **无法即时吊销**：把 access token 有效期设短（如 1 小时），注销时前端删 token 即可；若将来有"强制下线"需求，再加 Redis 黑名单。当前系统不需要服务端注销。

> 如果团队更看重"注销即时生效 + 凭证 JS 不可读"、且能接受单实例内存会话（或愿意加 Redis），那么 Session + HttpOnly Cookie 在**安全性上反而是更省心的选择**，尤其是配合 Spring Security 默认配置。两条路线后端 Controller 层的改造几乎一样，区别只在凭证载体和 Filter 实现。下文以 JWT 为主线展开。

### 10.2 后端逐文件改动

#### ① [pom.xml](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/backend/pom.xml) — 加依赖

- 新增 `spring-boot-starter-security`：提供 SecurityFilterChain、方法级安全、PasswordEncoder、AuthenticationManager。
- 新增 JJWT 依赖（`jjwt-api` / `jjwt-impl` / `jjwt-jackson`）：用于签发和校验 JWT。也可以自己用 `javax.crypto.Mac` + HMAC-SHA256 手写，但 JJWT 是事实标准。
- `spring-boot-starter-validation` 已存在，留给 DTO 校验用。

#### ② 新建 `com.supermarket.security` 包

需要新增 4 个类：

- **`JwtUtil.java`**：集中管理 JWT 的签发与解析。
  - `generateToken(username, role)`：用 HMAC-SHA 密钥签名，subject 放 username，claim 放 role，设置过期时间。
  - `getUsernameFromToken(token)` / `getRoleFromToken(token)`：解析 claim。
  - `validateToken(token)`：验签 + 验过期。
  - 密钥和过期时间从 `application.properties` 注入（见 ⑥）。
- **`JwtAuthenticationFilter.java`**（继承 `OncePerRequestFilter`）：这是整个鉴权的核心关卡。
  - 每次请求从 `Authorization: Bearer <token>` 头取 token。
  - 校验通过后，构造 `UsernamePasswordAuthenticationToken(username, null, authorities)`，authorities 由 role 转成 `ROLE_USER`/`ROLE_ADMIN`，塞进 `SecurityContextHolder.getContext().setAuthentication(...)`。
  - 没带 token 或校验失败时**不抛异常、不重定向**——直接放行到过滤链后面，由 SecurityConfig 决定这个路径是否需要认证；需要认证的会被 EntryPoint 拦成 401，不需要的（如 `/api/auth/login`）照常访问。
- **`JwtAuthenticationEntryPoint.java`**（实现 `AuthenticationEntryPoint`）：未认证请求访问受保护接口时，统一返回 `401` + JSON `{"message":"未登录或登录已过期"}`，而不是 Spring 默认的 403 或跳转登录页。
- **`CustomUserDetailsService.java`**（实现 `UserDetailsService`）：按用户名从 [UserRepository](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/backend/src/main/java/com/supermarket/repository/UserRepository.java) 查用户，返回带 `ROLE_xxx` 权限的 Spring Security `User`。它主要给登录时的 `AuthenticationManager` 用（用 BCrypt 验密），JWT Filter 本身不查库、保持无状态。

#### ③ 新建 [config/SecurityConfig.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/backend/src/main/java/com/supermarket/config) — 安全总配置

用 `@Configuration + @EnableWebSecurity + @EnableMethodSecurity`（后者开启 `@PreAuthorize`）。声明一个 `SecurityFilterChain` Bean：

- `csrf.disable()`：纯前后端分离、用 Header 传 token，不需要 CSRF token。
- `sessionManagement.sessionCreationPolicy(STATELESS)`：不创建 HttpSession，确保无状态。
- `authorizeHttpRequests`：
  - `POST /api/auth/login`、`POST /api/auth/register` 放行（permitAll）。
  - 其余 `/api/**` 全部 `authenticated()`。
  - 可按需把静态资源/error 放行。
- `exceptionHandling.authenticationEntryPoint(jwtAuthEntryPoint)`：未认证时返回 401 JSON。
- `addFilterBefore(jwtAuthFilter, UsernamePasswordAuthenticationFilter.class)`：把 JWT 过滤器插在用户名密码过滤器之前。
- `cors.configurationSource(...)`：集中配置允许的来源（开发环境 `http://localhost:3000`，生产环境用实际域名），允许 `Authorization` 头。**替代掉现在散落在各 Controller 上的 `@CrossOrigin(origins="*")`。**
- 额外声明两个 Bean：
  - `PasswordEncoder` → `new BCryptPasswordEncoder()`（根治明文密码）。
  - `AuthenticationManager`（通过 `AuthenticationConfiguration` 获取或装配 DaoAuthenticationProvider），供登录接口调用。

#### ④ [AuthController.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/backend/src/main/java/com/supermarket/controller/AuthController.java) — 登录发 token、注册收窄

- **登录 `/login`**：
  - 不再自己 `findByUsername` + `equals` 比明文。改为调用 `authenticationManager.authenticate(new UsernamePasswordAuthenticationToken(username, password))`，由 `CustomUserDetailsService` 查用户、`BCryptPasswordEncoder` 验密。
  - 认证成功后，用 `JwtUtil.generateToken(username, role)` 签发 JWT。
  - 返回体从 `{id, username, role}` 改为 `{token, id, username, role}`（多了 token 字段）。
- **注册 `/register`**：
  - **不再直接 `@RequestBody User`**（这是越权自带 role 的根因）。新建一个 `RegisterRequest` DTO，只含 `username`、`password`，加 `@NotBlank`/长度校验。
  - 服务端 `new User()` 后**强制 `setRole("USER")`**，忽略请求里任何 role 字段；密码用 `passwordEncoder.encode(rawPassword)` 加密后再存。
  - 用户名重复仍返回 400。
- **新增 `GET /api/auth/me`**：从 `SecurityContextHolder` 取当前认证用户，返回 `{id, username, role}`。前端刷新页面时用它校验 token 是否还有效、顺便拿最新角色，而不是盲信 localStorage。
- 去掉类上的 `@CrossOrigin(origins="*")`（CORS 已在 SecurityConfig 统一管）。

#### ⑤ 新建 `dto` 包

- `LoginRequest`（username、password，带 `@NotBlank`）。
- `RegisterRequest`（username、password，带 `@NotBlank`、`@Size(min=6)`）。
- `AuthResponse`（token、id、username、role）。
用 DTO 而不是直接绑定实体，能从根本上杜绝"请求体带什么字段就存什么字段"的批量赋值问题。

#### ⑥ [application.properties](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/backend/src/main/resources/application.properties) — JWT 配置

- 新增 `app.jwt.secret`（HMAC 密钥，**必须从环境变量注入**，不能硬编码进仓库；本地开发可用默认值但要在文档里标注）。
- 新增 `app.jwt.expiration-ms=3600000`（access token 1 小时有效期）。

#### ⑦ [DataInitializer.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/backend/src/main/java/com/supermarket/config/DataInitializer.java#L36-L48) — 种子密码必须加密

这是一个**容易漏、漏了就登录失败**的点：当前 admin/123456、user/123456 是明文写库的。引入 BCrypt 后，`CustomUserDetailsService` 拿到的密码哈希和提交的明文永远对不上。必须把这两处改成 `passwordEncoder.encode("123456")` 后再保存（注入 PasswordEncoder）。

#### ⑧ 四个业务 Controller — 去掉开放 CORS、加角色注解

涉及 [ProductController](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/backend/src/main/java/com/supermarket/controller/ProductController.java)、[CategoryController](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/backend/src/main/java/com/supermarket/controller/CategoryController.java)、[SupplierController](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/backend/src/main/java/com/supermarket/controller/SupplierController.java)、[PurchaseOrderController](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/backend/src/main/java/com/supermarket/controller/PurchaseOrderController.java)：

- 去掉每个类上的 `@CrossOrigin(origins = "*")`。
- 方法本身**不需要写任何认证代码**——SecurityConfig 里 `anyRequest().authenticated()` 已经保证未带 token 的请求进不来。这 16 个业务接口（商品 5 + 分类 3 + 供应商 4 + 采购单 4）由此从"匿名可直调"变成"必须带有效 JWT"。
- 角色细化（可选但推荐）：
  - 读接口（GET）：`@PreAuthorize("hasAnyRole('USER','ADMIN')")`，普通登录用户都能看。
  - 写接口（POST/PUT/DELETE）：`@PreAuthorize("hasRole('ADMIN')")`，只有管理员能增删改。这符合"超市管理系统里普通员工查商品、管理员改数据"的直觉。
  - **注意联动前端**：当前前端没有角色区分，种子里 `user` 账号是 USER 角色。一旦写接口限定 ADMIN，用 user 登录后点"添加/删除"会收到 403。要么给前端加角色按钮控制（推荐），要么第一阶段先只要求"登录即可"、暂不按角色限写，等 UI 跟上再开。这是个需要产品决策的点，不能闷头改。

#### ⑨ 新建 `config/GlobalExceptionHandler.java`（推荐，非必须）

当前后端没有任何 `@RestControllerAdvice`，Controller 里 `orElseThrow(() -> new RuntimeException("... not found"))` 会变成 Spring 默认的 500 错误体。建议加一个全局异常处理器：

- 处理 `AccessDeniedException` → 403 + `{"message":"没有权限"}`。
- 处理 `RuntimeException`/实体不存在 → 404 或 400。
- 处理 `MethodArgumentNotValidException`（DTO 校验失败）→ 400 + 字段错误信息。

这不是鉴权本身，但它让 401/403 的返回格式和前端 [request.js](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/frontend/src/api/request.js) 的错误提示能对得上。

### 10.3 前端逐文件改动

#### ① [api/request.js](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/frontend/src/api/request.js) — 最关键的前端改动

- **加请求拦截器**：每次发请求前从 localStorage 读 token，存在就 `config.headers.Authorization = 'Bearer ' + token`。这是把"登录状态"传给后端的唯一载体。
- **改响应拦截器**：在现有错误提示基础上，判断 `error.response?.status === 401`，则清掉本地 token/user 并跳转到 `/login`；403 可提示"没有权限"。这样一旦 token 过期或被篡改，用户会被立即踢回登录页，而不是停在一个不停报错的页面。
- 跳转需要用到 router 实例。可直接 `import router from '../router'`（注意循环依赖：router 不引用 request，所以不会环），或用 `window.location.href = '/login'` 兜底。

#### ② [views/Login.vue](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/frontend/src/views/Login.vue#L82-L94) — 存 token 而不是只存 user

- `handleLogin` 里后端现在返回 `{token, id, username, role}`。改为：
  - `localStorage.setItem('token', data.token)`
  - `localStorage.setItem('user', JSON.stringify({id, username, role}))`（user 信息留着给顶栏显示，但它不再是"登录凭证"，只是展示用缓存）。
- 注册成功后的逻辑不用大改，但可让注册接口返回后自动用新账号登录（拿 token），体验更顺。

#### ③ [router/index.js](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/frontend/src/router/index.js#L42-L52) — 守卫改判 token

- 把 `localStorage.getItem('user')` 改成 `localStorage.getItem('token')`，判断是否存在。
- **认知要摆正**：路由守卫只是"避免未登录用户看到页面框架"的 UX 优化，**它不是安全边界**——真正的安全边界是后端返回 401。即使守卫被绕过（比如直接塞假 token），后端 JWT Filter 校验签名失败照样拒绝。
- 如果后续做了 ADMIN 专属页面，可在路由 `meta.roles` 里加角色要求，守卫里读 localStorage user 的 role 做前端级菜单/路由控制（同样只是 UX，不是安全）。

#### ④ [App.vue](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/frontend/src/App.vue#L84-L87) — 退出登录取 token、可选调 /me

- `handleLogout`：除了 `removeItem('user')`，还要 `removeItem('token')`，然后跳登录页。纯 JWT 下不需要调后端注销接口；若将来加了 token 黑名单，再调 `POST /api/auth/logout`。
- 顶栏用户名的 computed 仍然可以从 localStorage 的 user 读，但更稳妥的是应用启动时调一次 `/api/auth/me` 校验 token 并刷新用户信息（可在 `main.js` 或 App.vue 的 `onMounted` 里做），失败则清登录态。这样能避免"localStorage 里有个过期 token，守卫放行进了页面，结果一调接口全 401"的尴尬。
- 可选：根据 role 隐藏/禁用"添加/删除"按钮（配合后端 ADMIN 限制），属于 UI 层增强。

#### ⑤ 新建 `src/utils/auth.js`（推荐，非必须）

当前 token/user 的读写散落在 Login、router、App、request 四处。建议抽一个小模块导出 `getToken/setToken/clearToken/getUser/setUser/isLoggedIn`，统一 localStorage 的 key 名和 JSON 解析，避免某个文件拼错 key 或忘记 JSON.parse 导致之前推演里的白屏问题。项目没有 Pinia/Vuex，这个轻量模块就够用，不必为此引入状态管理库。

#### ⑥ [vite.config.js](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/frontend/vite.config.js) / [nginx.conf](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/frontend/nginx.conf)

- 开发环境 Vite proxy 已经把 `/api` 转发到 8080，是同源转发，Authorization 头会自动带过去，不用改。
- 生产 nginx 已经把 `/api` 反代到 backend，同样不用改结构；只要保证后端 SecurityConfig 的 CORS 允许前端域名即可（同源部署其实连 CORS 都不触发，但保留配置以防端口/域名分离）。

### 10.4 改动顺序与优先级（对照接口清单）

按"先把最大的洞堵上"排序，前三步必须同批上线，否则系统仍不可用。

**P0 —— 第一批，必须一起做（堵住 16 个匿名业务接口）：**

1. 后端：pom 加 security + jjwt；新建 security 包 4 个类 + SecurityConfig。这一步让 SecurityConfig 里 `anyRequest().authenticated()` 生效，第七节清单里的 **第 3–18 号接口（商品 5、分类 3、供应商 4、采购单 4）全部从"匿名可直调"变成"必须带 token"**。
2. 后端：改造 [AuthController](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/backend/src/main/java/com/supermarket/controller/AuthController.java) 登录接口签发 JWT（清单第 2 号）。没有这步，前端拿不到 token，P0.1 会把所有人挡在外面。
3. 前端：[request.js](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/frontend/src/api/request.js) 加请求拦截器带 token + 401 跳登录；[Login.vue](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/frontend/src/views/Login.vue) 存 token；[router/index.js](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/frontend/src/router/index.js) 改判 token；[App.vue](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/frontend/src/App.vue) 退出清 token。

这三步合在一起才构成完整闭环：登录拿 token → 每次请求带 token → 后端 Filter 校验 → 失败 401 → 前端踢回登录。

**P1 —— 第二批，和 P0 同一个 PR 做掉（修凭证安全，否则 P0 留隐患）：**

4. `BCryptPasswordEncoder` 加密密码：SecurityConfig 声明 Bean、登录改用 AuthenticationManager 验密、注册 encode 后存库、**[DataInitializer](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/backend/src/main/java/com/supermarket/config/DataInitializer.java) 种子密码也 encode**。
5. 注册接口改用 `RegisterRequest` DTO，强制 role=USER，堵住匿名建管理员（清单第 1 号的越权点）。

P1 严格说应该和 P0 一起，因为你不可能上线一个"密码还是明文"的鉴权系统。

**P2 —— 第三批，上线后跟进（加固与体验）：**

6. 收紧 CORS：删掉四个业务 Controller 上的 `@CrossOrigin("*")`，在 SecurityConfig 里配明确来源。
7. 角色细粒度：写接口加 `@PreAuthorize("hasRole('ADMIN')")`，前端按 role 控制按钮显隐；同步确认 user/admin 账号的角色分配符合预期。
8. 新增 `GET /api/auth/me`，前端启动时校验 token 有效性。
9. 加 `GlobalExceptionHandler`，统一 403/404/校验错误的返回格式。
10. 给实体字段加 Bean Validation 注解（`@NotBlank` 等）并在 Controller 参数加 `@Valid`，避免非法数据落库。
11. token 存储加固：评估从 localStorage 迁移到"内存 access token + HttpOnly refresh token"，降低 XSS 窃取影响；按需引入 refresh 机制和注销黑名单。
12. 决定采购单接口（清单 15–18 号）的去留：要么补前端页面，要么在未启用前从路由/Controller 层收窄，减少无用攻击面。

### 10.5 附带问题要不要一起处理

| 附带问题 | 要不要一起改 | 放在哪一批 | 说明 |
|----------|--------------|-----------|------|
| 密码明文存储 | **必须改** | P1 | 和鉴权是一体的，BCrypt 是 PasswordEncoder 的默认选择；别忘了 [DataInitializer](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/backend/src/main/java/com/supermarket/config/DataInitializer.java) 的种子账号，否则登录全挂 |
| 注册自带 role 越权 | **必须改** | P1 | 改用 RegisterRequest DTO + 服务端强制 USER，顺手在改 AuthController 时一起做 |
| CORS `*` 全开 | **要改** | P2 | P0 阶段 `@CrossOrigin("*")` 还在不影响功能，但它是"任意网站可跨域调 API"的根源；迁移到 SecurityConfig 集中配置后删掉注解即可，成本很低 |
| `role` 字段前端没用、后端没鉴权 | **部分改** | P2 | 鉴权补上后 role 才有意义；先在后端用 `@PreAuthorize` 落地，前端按钮控制跟上 |
| 采购单接口无前端页面 | **建议处理** | P2 | P0 的 SecurityConfig 会自动保护它们，但"没人用却暴露着"仍不好；定去留 |
| 无全局异常处理 | 建议改 | P2 | 不是安全漏洞，但让 401/403 返回体统一，前端错误提示更准 |
| 实体无入参校验 | 建议改 | P2 | `@Valid` + DTO 注解，防脏数据，非鉴权范畴 |
| localStorage 可被 XSS 窃取 token | 分阶段 | P2+ | 第一版接受风险（和现状一致）；第二版用 HttpOnly refresh token 加固 |

### 10.6 改完之后的链路（用来对照验收）

整改完成后，完整链路应该是：

1. **登录**：前端 POST `/api/auth/login` → 后端 BCrypt 验密通过 → 签发带 username/role 签名的 JWT → 前端存 `token`。
2. **打开页面**：路由守卫检查 token 是否存在（UX 层）；应用可调 `/api/auth/me` 让后端验签确认 token 有效（安全层）。
3. **每次拉数据**：[request.js](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-steve/frontend/src/api/request.js) 请求拦截器自动加 `Authorization: Bearer <token>` → 后端 `JwtAuthenticationFilter` 验签、验过期，通过则把用户身份放进 SecurityContext → Controller 正常返回。
4. **匿名直调 API**：没带 token → Filter 不设置身份 → SecurityConfig 发现访问受保护资源 → `JwtAuthenticationEntryPoint` 返回 401 JSON → 前端响应拦截器踢回登录页。
5. **伪造/过期 token**：签名校验失败或已过期 → 同样 401，控制台塞假字符串不再能进系统（即便路由守卫被蒙混，后端也会拒绝每个数据请求）。
6. **越权操作**：普通 USER 调写接口 → `@PreAuthorize("hasRole('ADMIN')")` 拒绝 → 403。
7. **退出**：前端删 token → 后续请求不带凭证 → 401 → 回登录页。

到这一步，"登录状态"才真正由后端签发、后端校验，前端的路由守卫退回到它应有的角色——体验优化，而不是唯一防线。
