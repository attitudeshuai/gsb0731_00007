# 登录状态链路分析（超市管理系统）

> 本文只做分析，不改任何代码。所有结论以仓库实际实现为准。
> 结论先行：**这套系统的"登录状态"完全是前端的一场独角戏。后端在登录成功后没有下发任何凭证，商品/分类/供应商接口也完全不校验任何登录状态。前端的路由拦截只是"障眼法"，绕过它几乎零成本。**

---

## 一、先给结论

1. 后端登录成功后**不发任何凭证**：没有 JWT，没有 Session/Cookie，只返回一段用户信息 JSON（id / username / role）。
2. 前端把这段用户信息 JSON 原样塞进 `localStorage` 的 `user` 键，**这就是它认定的"登录状态"**。
3. 之后每打开一个页面，前端路由守卫只判断 `localStorage` 里有没有 `user` 这个 key，**有就放行，没有就跳登录页**。
4. 每次拉数据（商品/分类/供应商）时，请求里**根本不带任何身份信息**（没有 Authorization 头、没有 Cookie、没有 user 字段）。
5. 后端的商品/分类/供应商接口**完全不认这个状态**——它们对谁都开放，任何人直接调 API 都能读写删。

一句话：登录状态只存在于浏览器的 localStorage 里，前端自娱自乐地维持它；后端对业务接口没有任何鉴权。

---

## 二、从"登录成功那一刻"开始，逐层追链路

### 第 0 层：登录请求

前端登录表单提交，调用 `/api/auth/login`。

- 前端：[Login.vue](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/frontend/src/views/Login.vue#L82-L94)

```js
const data = await request.post('/auth/login', loginForm);
localStorage.setItem('user', JSON.stringify(data));
```

### 第 1 层：后端如何处理登录（关键点：不发凭证）

- 后端：[AuthController.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/backend/src/main/java/com/supermarket/controller/AuthController.java#L35-L51)

后端做的事：
1. 按用户名查用户；
2. **明文比对密码**（`user.getPassword().equals(loginUser.getPassword())`，没有任何加密/哈希，注册时也是明文存库，见 [AuthController.java#L28-L32](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/backend/src/main/java/com/supermarket/controller/AuthController.java#L28-L32)）；
3. 成功后返回一个 Map：`{ id, username, role }`。

**注意：这里没有 `HttpSession`、没有 `Set-Cookie`、没有生成任何 token。** 返回的只是"这个人是谁"的普通业务数据，不是一张可以在后续请求里出示的"通行证"。也就是说，登录接口和一个普通查询接口在"凭证"意义上没有区别。

### 第 2 层：前端把返回值当成"登录状态"存起来

前端拿到返回的 `{ id, username, role }`，直接：

```js
localStorage.setItem('user', JSON.stringify(data));
```

从此，浏览器 `localStorage` 里的 `user` 这个 key **就是这套系统全部的"登录状态"**。它：
- 没有有效期（不会过期）；
- 没有签名（内容可随意伪造）；
- 只存在于这一个浏览器里。

### 第 3 层：每次"打开页面"如何拦截

页面切换时，路由守卫做判断：

- 前端：[router/index.js](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/frontend/src/router/index.js#L42-L52)

```js
router.beforeEach((to, from, next) => {
    const user = localStorage.getItem('user');
    if (to.meta.requiresAuth && !user) {
        next('/login');           // 没有 user -> 踢回登录页
    } else if (to.path === '/login' && user) {
        next('/products');        // 有 user 还想去登录页 -> 送进主页
    } else {
        next();
    }
});
```

配合路由表里 `/products`、`/categories`、`/suppliers` 都标了 `meta: { requiresAuth: true }`（[router/index.js#L17-L34](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/frontend/src/router/index.js#L17-L34)）。

**判断逻辑只有一句：`localStorage` 里存不存在 `user` 这个 key。** 它不校验里面内容是否合法、是否过期、后端是否认可——只看"有没有"。

顶部导航栏显示的用户名、退出登录，也都是直接读/删这个 localStorage，见 [App.vue#L76-L87](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/frontend/src/App.vue#L76-L87)。退出登录就是 `localStorage.removeItem('user')`。

### 第 4 层：每次"拉数据"时，请求里带了什么身份信息？——什么都没带

所有业务请求都走统一的 axios 实例：

- 前端：[api/request.js](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/frontend/src/api/request.js)

```js
const instance = axios.create({
    baseURL: '/api',
    timeout: 10000
});
instance.interceptors.response.use(
    response => response.data,
    error => { /* 只做错误提示 */ }
);
```

**关键：这里只有一个"响应拦截器"（用来统一弹错误提示），没有"请求拦截器"。** 也就是说，发请求时**不会**把 localStorage 里的 user 加到请求头（比如 `Authorization`）里，也不会带任何 token。

各页面拉数据的调用（[ProductList.vue](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/frontend/src/views/ProductList.vue#L119)、[CategoryManager.vue](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/frontend/src/views/CategoryManager.vue#L68)、[SupplierManager.vue](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/frontend/src/views/SupplierManager.vue#L85)）都只是 `request.get('/products')` 之类，没有任何一处附带身份信息。

请求经 nginx 反代转发到后端（[nginx.conf#L11-L17](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/frontend/nginx.conf#L11-L17)），nginx 只加了 `X-Real-IP` 等转发头，也不涉及任何鉴权。

### 第 5 层：后端业务接口认不认这个"登录状态"？——完全不认

三个业务 Controller 全都是"裸奔"的 CRUD，没有任何鉴权：

- 商品：[ProductController.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/backend/src/main/java/com/supermarket/controller/ProductController.java) — 增删改查全开放
- 分类：[CategoryController.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/backend/src/main/java/com/supermarket/controller/CategoryController.java)
- 供应商：[SupplierController.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/backend/src/main/java/com/supermarket/controller/SupplierController.java)

这些方法体里**只有对 Repository 的直接调用**，没有读取任何请求头、没有校验 user/role、没有 `@PreAuthorize`。

而且**整个后端项目里根本不存在鉴权机制**：
- [pom.xml](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/backend/pom.xml) **没有引入 `spring-boot-starter-security`**，也没有任何 JWT 库；
- 全仓库搜不到任何 `Filter`、`HandlerInterceptor`、`WebMvcConfigurer`、`@PreAuthorize`、`SecurityConfig`；
- 所有 Controller 都带 `@CrossOrigin(origins = "*")`，对任意来源开放跨域。

**所以后端对"登录状态"是零感知的。** 前端登录与否，对后端业务接口没有任何影响。

---

## 三、链路全景图

```
[登录成功那一刻]
  前端 POST /api/auth/login
        │
        ▼
  AuthController.login()  ── 明文比对密码，返回 {id,username,role}
        │  （注意：没有 token / 没有 Cookie / 没有 Session）
        ▼
  前端 localStorage.setItem('user', {id,username,role})
        │  ← 这就是全部的"登录状态"，无签名、无过期
        │
        ├──[每次打开页面]──► router.beforeEach 只看 localStorage 有没有 'user'
        │                     有 → 放行；无 → 跳 /login
        │
        └──[每次拉数据]────► request.get/post/... 
                              请求里不带任何身份信息（无 Authorization、无 Cookie）
                                    │
                                    ▼ nginx 反代（不鉴权）
                                    ▼
                              Product/Category/Supplier Controller
                                    └─ 直接查库/写库，对谁都开放
```

---

## 四、什么情况下会"绕过它出问题"

由于"登录状态"只在前端、后端不鉴权，以下场景都能轻松绕过，且直接造成越权：

1. **直接调后端 API，完全无视前端**
   任何人用 curl / Postman 直接请求 `GET /api/products`、`POST /api/products`、`DELETE /api/products/{id}`、`DELETE /api/categories/{id}`、`DELETE /api/suppliers/{id}`，**根本不用登录**就能读、能改、能删所有数据。前端那套拦截等于不存在——因为它拦的是浏览器页面，拦不住直接打接口。这是最严重的问题。

2. **手动往 localStorage 塞一个假的 user 就能进系统**
   在浏览器控制台执行 `localStorage.setItem('user', '{}')`（甚至 `'x'`），刷新页面，路由守卫看到 key 存在就放行。**无需真实账号密码即可进入所有管理页面。** 因为守卫只判断 key 是否存在，不校验内容、更不问后端。

3. **role 字段形同虚设，没有权限分级**
   User 里有 `role`（USER/ADMIN，见 [User.java#L21-L22](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/backend/src/main/java/com/supermarket/model/User.java#L21-L22)），但前后端都没有任何地方根据 role 做权限区分。普通用户和管理员能做的事完全一样，删除接口对所有人开放。

4. **"退出登录"只是本地删 key，服务端毫无感知**
   `handleLogout` 只是 `localStorage.removeItem('user')`。因为后端本就没有会话，所谓"退出"只是清掉浏览器里的标记，不存在服务端会话失效的概念。

5. **"登录状态"永不过期**
   localStorage 里的 user 不设过期时间。只要不手动退出、不清浏览器数据，这个"登录态"永久有效。

6. **凭证可被任意读取/伪造**
   localStorage 内容对同源 JS 完全可读可写，且返回的用户信息里若含 role 等敏感信息也是明文存放，容易被 XSS 等手段读取或篡改。加上密码在库里明文存储、登录时明文比对，一旦库泄露风险极大。

---

## 五、总结（一句话回答你的疑问）

- **"后端登录成功后没给前端发凭证"——你观察得对。** 系统压根没有凭证机制。
- **这个"登录状态"是怎么传下去维持住的？** 靠前端把登录接口返回的用户信息存进 localStorage，之后每次打开页面由路由守卫检查这个 key 是否存在来"维持"；它**没有向后端传递、也没有被后端验证**。
- **后端商品/分类/供应商接口认不认这个状态？** 完全不认，接口对所有人开放。
- **什么情况会绕过出问题？** 只要直接请求后端 API，或在 localStorage 里手动塞个假值，就能完全绕过前端拦截，实现未授权的读/写/删。前端拦截仅提升"页面层面的体验",不构成任何真正的安全边界。

> 定性：真正的鉴权应在后端。当前实现把"鉴权"放在了前端，且后端零校验，属于典型的"前端假鉴权 + 后端无鉴权"。本文仅为分析，未对代码做任何修改。

---

# 【第二轮补充】后端接口全清单 + 伪造 user 露馅推演

> 说明：以下为第二轮追加内容。**上面第一轮的所有结论保持不动。** 推演过程中若发现与上轮结论对不上的地方，统一在文末【第七节：与上轮结论的差异澄清】单独标出。

## 六、后端接口全清单（认证 / 商品 / 分类 / 供应商 / 采购单）

前置事实（对整张表都成立）：
- 后端**没有引入 Spring Security**，全仓库无 Filter / HandlerInterceptor / WebMvcConfigurer / `@PreAuthorize` / SecurityConfig。
- 每个 Controller 都带 `@CrossOrigin(origins = "*")`，对任意来源开放。
- 所以下表中**每一个接口的"身份校验"都是"无"，匿名直接调都能通**。差别只在"能干啥"。
- 演示数据由 [DataInitializer.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/backend/src/main/java/com/supermarket/config/DataInitializer.java) 初始化：内置用户 `admin/123456`(ADMIN)、`user/123456`(USER)，均**明文入库**。

### 6.1 认证接口 —— [AuthController.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/backend/src/main/java/com/supermarket/controller/AuthController.java)（base：`/api/auth`）

| 方法 | 路径 | 身份校验 | 匿名能否通 | 通了能干啥 |
|---|---|---|---|---|
| POST | `/api/auth/register` | 无 | 能 | **任意匿名注册新账号**。请求体是完整 User，`role` 由前端传入且后端不校验 → **可直接注册一个 `role=ADMIN` 的账号**。用户名重复才报错。 |
| POST | `/api/auth/login` | 无 | 能 | 明文比对密码。成功返回 `{id,username,role}`，**不下发任何凭证**。可被无限次暴力尝试（无锁定、无限流、无验证码）。 |

### 6.2 商品接口 —— [ProductController.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/backend/src/main/java/com/supermarket/controller/ProductController.java)（base：`/api/products`）

| 方法 | 路径 | 身份校验 | 匿名能否通 | 通了能干啥 |
|---|---|---|---|---|
| GET | `/api/products?name=` | 无 | 能 | **读全部商品**（名称、条形码、价格、库存、所属分类），支持按名模糊查。 |
| GET | `/api/products/category/{categoryId}` | 无 | 能 | 按分类读商品列表。 |
| POST | `/api/products` | 无 | 能 | **任意新增商品**（价格、库存等随意填）。 |
| PUT | `/api/products/{id}` | 无 | 能 | **任意改任意商品**——改名、改价、**改库存**、改条形码、改归属分类。找不到 id 抛 RuntimeException。 |
| DELETE | `/api/products/{id}` | 无 | 能 | **任意删任意商品**。删不存在的 id 也返回 200（`deleteById` 不校验存在性）。 |

### 6.3 分类接口 —— [CategoryController.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/backend/src/main/java/com/supermarket/controller/CategoryController.java)（base：`/api/categories`）

| 方法 | 路径 | 身份校验 | 匿名能否通 | 通了能干啥 |
|---|---|---|---|---|
| GET | `/api/categories` | 无 | 能 | 读全部分类（名称、描述）。 |
| POST | `/api/categories` | 无 | 能 | **任意新增分类**（name 唯一，重名会触发 DB 唯一约束报错）。 |
| DELETE | `/api/categories/{id}` | 无 | 能 | **任意删分类**。注意：Product 通过 `category_id` 外键关联，删被引用的分类可能触发外键约束异常或留下悬挂引用（取决于 DB 外键设置）。 |

### 6.4 供应商接口 —— [SupplierController.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/backend/src/main/java/com/supermarket/controller/SupplierController.java)（base：`/api/suppliers`）

| 方法 | 路径 | 身份校验 | 匿名能否通 | 通了能干啥 |
|---|---|---|---|---|
| GET | `/api/suppliers` | 无 | 能 | **读全部供应商**，含**联系人、电话、地址等敏感 PII**。 |
| POST | `/api/suppliers` | 无 | 能 | 任意新增供应商。 |
| PUT | `/api/suppliers/{id}` | 无 | 能 | **任意改任意供应商**（名称/联系人/电话/地址）。找不到 id 抛 RuntimeException。 |
| DELETE | `/api/suppliers/{id}` | 无 | 能 | **任意删任意供应商**。 |

### 6.5 采购单接口 —— [PurchaseOrderController.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/backend/src/main/java/com/supermarket/controller/PurchaseOrderController.java)（base：`/api/orders`）

| 方法 | 路径 | 身份校验 | 匿名能否通 | 通了能干啥 |
|---|---|---|---|---|
| GET | `/api/orders?status=` | 无 | 能 | **读全部采购单**（供应商、商品、数量、单价、总额、状态），支持按 status 过滤。**含供应商成本价等经营敏感数据**。 |
| POST | `/api/orders` | 无 | 能 | **任意新建采购单**，金额/数量/状态随意填。 |
| PUT | `/api/orders/{id}` | 无 | 能 | **任意改采购单**，包括直接把 `status` 改成 COMPLETED/CANCELLED（无状态机校验、无审批）。 |
| DELETE | `/api/orders/{id}` | 无 | 能 | **任意删采购单**。 |

> ⚠️ **采购单是本轮新暴露的重点**：这套接口在后端完整存在且同样零鉴权，但**前端根本没有对应页面/路由**（[router/index.js](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/frontend/src/router/index.js) 里没有 `/orders`，`views/` 下也没有采购单页面）。也就是说，**采购单数据只能通过直接打 API 访问**——它甚至连"前端假鉴权"这层障眼法都没有，是彻底裸露的一组读写删接口，最容易被忽视。

### 6.6 汇总
- 接口总数：**16 个**（认证 2 + 商品 5 + 分类 3 + 供应商 4 + 采购单 4，其中商品/供应商各含 1 个额外查询接口）。
- 有身份校验的：**0 个**。
- 匿名可通的：**16 个（全部）**。
- 危害面：全库数据可被匿名**读 / 增 / 改 / 删**；可匿名注册 ADMIN 账号；供应商 PII 与采购单成本数据完全暴露。

---

## 七、伪造 user 进入页面后的"露馅 / 无感"逐点推演

场景：攻击者未登录，在浏览器控制台手动写入一个假的 `user`，然后访问 `/products`。

### 7.1 先厘清一个关键前提：塞什么值，结果完全不同

- 路由守卫 [router/index.js#L44-L45](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/frontend/src/router/index.js#L44-L45) 只做 `localStorage.getItem('user')` 真值判断——**任何非空字符串都能过守卫**。
- 但顶栏用户名 [App.vue#L76-L82](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/frontend/src/App.vue#L76-L82) 会执行 `JSON.parse(user).username`。这一步对塞进去的值很敏感：

| 塞入的值 | 能否过路由守卫 | App.vue 顶栏表现 |
|---|---|---|
| `'{}'`（合法 JSON，无 username） | 能 | `JSON.parse` 成功，`.username` 为 `undefined` → **顶栏用户名显示为空白**（图标后面没字） |
| `'{"username":"hacker"}'` | 能 | 顶栏显示 **hacker**（完全可自定义，毫无破绽） |
| `'x'`（非法 JSON） | 能过守卫 | `JSON.parse('x')` **抛 SyntaxError** → computed 报错，可能导致布局渲染异常/白屏（详见 7.3 与第八节） |

**干净的伪造姿势是塞合法 JSON**，例如 `localStorage.setItem('user','{"username":"hacker","role":"ADMIN"}')`。下面 7.2 / 7.3 均以"塞了合法 JSON"为前提推演。

### 7.2 完全无感的地方（伪造后和真登录几乎无差别）

1. **能正常进所有页面**：`/products`、`/categories`、`/suppliers` 全部放行，跳转无提示。
2. **顶栏用户名可完全自定义**：塞 `{"username":"hacker"}` 就显示 hacker，塞成 `admin` 就显示 admin，肉眼无法分辨真假。
3. **数据照常加载**：进入商品页，`onMounted` 里 `fetchProducts()` / `fetchCategories()`（[ProductList.vue#L194-L197](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/frontend/src/views/ProductList.vue#L194-L197)）照常成功——因为拉数据根本不带身份，后端也不校验。**列表、增删改一切功能正常可用。**
4. **增删改商品/分类/供应商全部生效**：所有写操作都会真实落库，页面提示"成功"，与真实用户毫无区别。
5. **刷新页面依旧在线**：localStorage 持久化，刷新后 user 还在，守卫继续放行。

### 7.3 会露馅 / 有破绽的地方

1. **顶栏用户名可能是空白**：如果只塞了 `'{}'`（没带 username），顶栏在用户图标后面**什么都不显示**——这是唯一一眼可疑的地方。但只要伪造者顺手带上 `username` 字段即可完全消除。
2. **点"退出登录"行为正常但"骗不到服务端"**：`handleLogout`（[App.vue#L84-L87](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/frontend/src/App.vue#L84-L87)）就是 `localStorage.removeItem('user')` + 跳登录页。点了会正常退出、回到登录页——**从 UI 看毫无异常**。它本来也不与后端交互，所以"伪造 user 退出登录"和"真用户退出登录"表现完全一致，不构成露馅点。
3. **`role` 字段在前端根本没人用**：伪造成 `role:"ADMIN"` 还是 `"USER"` 对前端显示/功能没有任何影响（前端无一处读取 role），既不会露馅、也无需伪造。
4. **塞非法 JSON 会露馅**：若塞入 `'x'` 这类非法 JSON，虽然能过路由守卫，但 App.vue 计算 username 时 `JSON.parse` 抛异常，可能导致主框架渲染出错。**这与上轮"塞 'x' 也能进"的说法存在偏差，见第八节。**

### 7.4 推演小结
伪造 user 进入后，**除了"顶栏用户名可能空白"这一处、以及"塞了非法 JSON 会渲染报错"这一处外，其余全部无感**：能进页面、能看数据、能增删改、刷新保持、退出正常。而且由于后端零鉴权，就算不伪造 user、直接打 API 也能干同样的事——伪造 user 只是为了"用图形界面操作"而已。

---

## 八、与上轮结论的差异澄清

上轮为求简洁，有一处说法需要修正/细化，其余结论均成立：

- **上轮说**："在浏览器控制台执行 `localStorage.setItem('user','x')`（甚至 `'x'`），刷新页面……即可进入所有管理页面。"
- **本轮更精确的结论**：`'x'` 这类**非法 JSON** 确实能通过路由守卫（守卫只判断 key 是否存在），但会导致 [App.vue#L78](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/frontend/src/App.vue#L78) 的 `JSON.parse(user)` 抛异常，进而可能使主框架渲染出错。**因此"干净、无破绽的伪造"应使用合法 JSON（如 `'{"username":"hacker"}'`）。** 这并不改变"前端拦截可被轻易绕过"的核心结论——绕过依然成立，只是姿势要选对；何况绕过前端根本不是必需，直接打后端 API 即可。

- 其余上轮结论（后端不发凭证、登录态只在 localStorage、后端业务接口零鉴权、退出仅本地删 key、登录态永不过期、密码明文等）**本轮全部复核无误，继续有效**。本轮新增的事实是：**采购单 `/api/orders` 同样零鉴权且前端无页面**，以及 **register 接口可匿名注册任意 role（含 ADMIN）**。

> 本轮仍为纯分析，未对任何代码做修改。

---

# 【第三轮补充】鉴权整改方案（只出方案，不动代码）

> 说明：本轮延续前两轮结论，给出"如何把鉴权真正补上"的落地方案。仍**不修改任何代码**，只描述每个文件/接口该动什么、为什么。前两轮所有结论保持不动。

## 九、路线选型：Token（JWT） vs Session

先做技术选型，因为它决定后面所有文件怎么改。

### 9.1 两条路线在本系统的对比

| 维度 | Session（服务端会话 + Cookie） | Token / JWT（无状态） |
|---|---|---|
| 前端存储 | 浏览器自动管理 Cookie，JS 不用碰 | 需前端自己存（现在就是 localStorage 存 user，改存 token） |
| 后端存储 | 需服务端保存会话（内存/Redis），当前是单体 + MySQL，加内存 session 即可 | 无状态，服务端不存，天然适配水平扩展 |
| 与现状的改动量 | 前端几乎不用改请求逻辑（Cookie 自动带），但要处理 CSRF | 前端要在 [request.js](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/frontend/src/api/request.js) 加请求拦截器塞 header |
| CSRF 风险 | Cookie 自动携带 → 需额外防 CSRF | Bearer header 手动携带 → 天然免疫 CSRF |
| 主动失效/登出 | 服务端删会话即可，登出干净 | JWT 签发后难吊销，需黑名单或短时效+刷新 token |
| 跨域 | 当前前后端经 nginx 同源反代（`/api` → backend），Cookie 可用；但 Controller 现在 `@CrossOrigin(*)`，与带凭证 Cookie 冲突 | 与 CORS 配合更自然 |

### 9.2 结论：本系统选 **JWT（无状态 Token）**

理由，针对这套系统的实际情况：

1. **前端现成的心智模型就是"自己存一份东西"**——现在 [Login.vue](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/frontend/src/views/Login.vue) 已经在 `localStorage.setItem('user', ...)`，改成存 token 是最小认知跳跃，路由守卫、退出登录的结构几乎不用重构。
2. **免 CSRF**：用 `Authorization: Bearer` 手动带 token，天然规避 CSRF，省掉一整套 CSRF token 机制。而如果走 Cookie Session，必须同时处理 CSRF，改动更大。
3. **CORS 全开的历史包袱**：现在所有 Controller 是 `@CrossOrigin(origins="*")`。Cookie + 凭证模式下浏览器禁止 `*` 通配，必须逐一收敛为白名单；Token 模式对这块要求相对宽松（虽然本方案仍建议收敛 CORS，见第十一节）。
4. 规模小、单体应用，不需要 Session 的服务端集中管理优势；反而 JWT 的无状态更省事。

> 取舍代价：JWT 主动登出/吊销较弱。本方案用 **短时效 access token（如 2 小时）** 降低风险；是否引入 refresh token 视需求而定，演示系统可先不做，登出仅前端删 token 即可（可接受，因为 token 短命）。

## 十、逐文件 / 逐接口改动方案

### 10.1 后端：新增鉴权基础设施（新增文件）

1. **引入依赖** —— [pom.xml](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/backend/pom.xml)
   - 加 `spring-boot-starter-security`（提供过滤器链、密码编码器、方法级鉴权）。
   - 加 JWT 库（如 `io.jsonwebtoken:jjwt`）用于签发/校验 token。
   - **为什么**：现在 pom 里完全没有 security 依赖，这是"后端零鉴权"的根因。

2. **新增 `JwtUtil`（工具类）**
   - 职责：用服务端密钥签发 token（载荷放 username、role、过期时间），以及校验/解析 token。
   - **为什么**：登录成功后要真正"下发凭证"，这是补上第一轮指出的"后端不发凭证"缺口的核心。

3. **新增 `JwtAuthenticationFilter`（OncePerRequestFilter）**
   - 职责：每个请求先从 `Authorization: Bearer xxx` 取 token → 校验 → 把用户身份放进 `SecurityContext`。校验失败则返回 401。
   - **为什么**：这是让"每次拉数据"真正被后端识别的关键。对应第一轮第 4、5 层——现在请求不带身份、后端也不认，这个 filter 把两端接上。

4. **新增 `SecurityConfig`（SecurityFilterChain）**
   - 职责：定义哪些路径放行、哪些要认证；注册上面的 JWT filter；配置 `PasswordEncoder`（BCrypt）；配置 CORS 白名单；关闭默认表单登录、设为无状态 session。
   - 关键规则：
     - `POST /api/auth/login`、`POST /api/auth/register` → **放行**（否则没法登录）。
     - 其余 `/api/**`（products / categories / suppliers / orders）→ **一律要求已认证**。
     - 写操作（POST/PUT/DELETE）建议进一步用 `@PreAuthorize("hasRole('ADMIN')")` 限制为 ADMIN，读操作允许登录用户（见 10.4 权限分级）。
   - **为什么**：这是把第六节那张"16 个接口全部匿名可通"的清单从根上堵死的中枢。

### 10.2 后端：登录 / 注册接口改动 —— [AuthController.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/backend/src/main/java/com/supermarket/controller/AuthController.java)

- **login**（现 L35-L51）：
  - 密码比对改为用 `PasswordEncoder.matches(raw, encoded)`，不再明文 `equals`。
  - 校验通过后**用 JwtUtil 签发 token**，返回体改为 `{ token, username, role }`（token 是新增的凭证，username/role 仅供前端展示）。
  - **为什么**：这是"从登录那一刻起真正产生一张通行证"的地方，直接修复第一轮核心问题。

- **register**（现 L20-L33）：
  - **忽略/剥离请求体里的 `role` 字段**，服务端强制设为 `USER`（要造 ADMIN 只能由已认证 ADMIN 走单独的管理接口，或后台/初始化脚本）。
  - 保存前用 `PasswordEncoder.encode()` 加密密码。
  - **为什么**：修复第二轮 6.1 指出的"匿名注册可自带 role=ADMIN"越权提权漏洞，以及明文存储问题。

### 10.3 后端：各业务 Controller 改动

对 [ProductController](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/backend/src/main/java/com/supermarket/controller/ProductController.java)、[CategoryController](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/backend/src/main/java/com/supermarket/controller/CategoryController.java)、[SupplierController](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/backend/src/main/java/com/supermarket/controller/SupplierController.java)、[PurchaseOrderController](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/backend/src/main/java/com/supermarket/controller/PurchaseOrderController.java)：

- **主要靠 SecurityConfig 统一拦截，Controller 本身不必逐个写鉴权代码**（这也是选 Security 过滤器链的好处——不用在 16 个方法里散落校验）。
- **删除各 Controller 上的 `@CrossOrigin(origins = "*")`**，改由 SecurityConfig 统一配 CORS 白名单（见第十一节）。
- 若要做读写权限分级，在写方法（POST/PUT/DELETE）上加 `@PreAuthorize("hasRole('ADMIN')")`。
- **PurchaseOrderController 尤其要确认被纳入拦截**：第二轮发现它前端无页面、最易被遗忘，方案里必须显式确认 `/api/orders/**` 落在"需认证"规则内。

### 10.4 后端：权限分级（可选但建议）

- User 已有 `role`（[User.java#L21-L22](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/backend/src/main/java/com/supermarket/model/User.java#L21-L22)），JWT 载荷带上 role，`SecurityConfig` 开启 `@EnableMethodSecurity`。
- 建议：读接口（GET）→ 登录即可；写接口（POST/PUT/DELETE）→ 仅 ADMIN。
- **为什么**：修复第二轮指出的"role 形同虚设、普通用户也能删数据"。

### 10.5 前端：请求层 —— [request.js](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/frontend/src/api/request.js)

- **新增请求拦截器**：每次请求前从 localStorage 读 token，塞进 `Authorization: Bearer ${token}` 头。
  - **为什么**：这是第二轮反复强调的缺口——现在只有响应拦截器、没有请求拦截器，所以请求从不带身份。这一步让前端每次拉数据都携带凭证。
- **增强响应拦截器**：拦截 401/403，自动清 token、跳登录页（应对 token 过期/失效）。
  - **为什么**：token 短时效，过期后要能优雅引导重新登录，而不是一直报错。

### 10.6 前端：登录页 —— [Login.vue](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/frontend/src/views/Login.vue)

- 登录成功后（现 L85-L86）改为存 **token**：`localStorage.setItem('token', data.token)`；用户名等展示信息可另存一个 key 或从 token 解析。
- **为什么**：把"凭证"和"展示信息"分离，前端拦截依赖的是真 token，而不是可随意伪造的 user 对象。

### 10.7 前端：路由守卫 —— [router/index.js](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/frontend/src/router/index.js)

- 守卫判断条件从"有没有 user"改为"有没有 token"（现 L44-L45）。
- **明确定位**：前端守卫**只负责体验**（没登录别进管理页），**不作为安全边界**——真正的关卡在后端 filter。第二轮的"塞个假值就进页面"之所以危险，是因为当时后端毫无防线；补上后端后，就算前端守卫被绕过，后端拿不到合法 token 一样 401。
- **为什么**：即便如此仍改守卫，是为了体验一致（避免进去后满屏 401）。核心防护已移到后端。

### 10.8 前端：顶栏与登出 —— [App.vue](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/frontend/src/App.vue)

- 用户名来源改为从 token/单独存的展示信息取（现 L76-L82 直接 `JSON.parse(user)`）。顺带修掉第二轮/第八节指出的"塞非法 JSON 会 `JSON.parse` 抛错"的健壮性问题（加 try/catch）。
- 登出（现 L84-L87）改为清 token。

## 十一、附带问题一并处理的建议

第一/二轮暴露的三个附带问题，**建议随本次一起处理**，因为它们和鉴权是同一条安全链，分开做会留窗口：

1. **明文密码（必须一起做）** —— [AuthController](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/backend/src/main/java/com/supermarket/controller/AuthController.java) + [DataInitializer.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/backend/src/main/java/com/supermarket/config/DataInitializer.java)
   - 引入 BCrypt `PasswordEncoder`，注册时 encode、登录时 matches。
   - DataInitializer 里内置的 `admin/123456`、`user/123456` 也要改成 encode 后写入，否则加密比对会失败、内置账号登不上。
   - **为什么必须一起**：一旦启用 BCrypt 比对，明文库数据会直接登录失败，二者必须同步改。

2. **注册可自带 role（必须一起做）** —— register 强制 role=USER（见 10.2）。这是**提权漏洞**，优先级等同鉴权本身，否则补了鉴权也能被匿名注册 ADMIN 绕过。

3. **CORS 全开（建议一起做）** —— 去掉各 Controller 的 `@CrossOrigin(origins="*")`，在 SecurityConfig 统一配可信来源白名单。
   - **为什么**：`*` 通配与"带凭证的鉴权请求"在浏览器策略上冲突，且过度放开来源；收敛后与本方案的 token 模式配套。生产可只允许前端部署域名。

## 十二、改动先后顺序（按前两轮接口清单，先堵最狠的）

优先级依据：**匿名可造成的破坏面 + 是否为提权入口**。

- **P0（必须最先堵，否则补了也白补）**
  1. 后端引入 Security + JWT filter + SecurityConfig（10.1）——总闸，让 16 个接口从"全裸"变"默认需认证"。
  2. register 剥离 role、强制 USER（10.2）——堵提权入口，否则匿名注册 ADMIN 可绕过一切。
  3. login 签发 token + BCrypt（10.2 + 11.1）——产生真凭证 + 修明文。

- **P1（紧接着堵写操作与最易忽视接口）**
  4. 确认 `/api/orders/**` 纳入拦截（10.3）——第二轮发现的"前端无页面、纯裸露"接口，最易漏。
  5. 各 Controller 的 **DELETE / PUT / POST** 加 ADMIN 限制（10.4）——删改破坏面最大：删商品、改库存改价、改采购单状态、删供应商。
  6. 去掉 `@CrossOrigin(*)`、统一 CORS 白名单（11.3）。

- **P2（前端配套 + 体验）**
  7. [request.js](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/frontend/src/api/request.js) 加请求拦截器带 token + 响应拦截 401（10.5）。
  8. [Login.vue](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/frontend/src/views/Login.vue) / [router/index.js](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/frontend/src/router/index.js) / [App.vue](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-natasha/frontend/src/App.vue) 改用 token（10.6-10.8）。

> 排序逻辑：P0 是"止血"——只要后端 filter + register 修好，即使前端一行没改，匿名破坏和提权就已被堵死；P1 收紧写权限和被遗忘的采购单；P2 让前端与新机制对齐、体验顺畅。**先后端后前端**，因为安全边界必须在后端，前端只是配套。

> 本轮仍为纯方案设计，未对任何代码做修改。
