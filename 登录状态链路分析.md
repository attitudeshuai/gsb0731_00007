# 登录状态链路分析（超市管理系统）

> 本文只做分析，不改任何代码。所有结论以仓库实际实现为准。
> 分析日期：2026-08-01

## 一句话结论

这个系统里的“登录状态”**只存在于前端浏览器里**，是一个纯前端的假象。
后端登录接口成功后**不下发任何凭证**（没有 token、没有 Cookie、没有 Session），
前端只是把登录返回的用户信息塞进 `localStorage`，之后所有页面拦截和菜单显示都靠读这份本地数据。
**商品、分类、供应商、采购单等所有业务接口在后端完全不校验登录状态**——任何人不登录、直接发 HTTP 请求就能读写全部数据。前端的登录拦截只挡“正常点页面的用户”，挡不住绕过浏览器的直接调用。

---

## 一、涉及的关键文件

| 层 | 文件 | 作用 |
|---|---|---|
| 后端 | [AuthController.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/backend/src/main/java/com/supermarket/controller/AuthController.java) | 登录 / 注册接口 |
| 后端 | [ProductController.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/backend/src/main/java/com/supermarket/controller/ProductController.java) | 商品接口 |
| 后端 | [CategoryController.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/backend/src/main/java/com/supermarket/controller/CategoryController.java) | 分类接口 |
| 后端 | [SupplierController.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/backend/src/main/java/com/supermarket/controller/SupplierController.java) | 供应商接口 |
| 后端 | [PurchaseOrderController.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/backend/src/main/java/com/supermarket/controller/PurchaseOrderController.java) | 采购单接口 |
| 后端 | [pom.xml](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/backend/pom.xml) | 依赖清单（无 Spring Security） |
| 前端 | [Login.vue](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/frontend/src/views/Login.vue) | 登录页，写入 localStorage |
| 前端 | [router/index.js](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/frontend/src/router/index.js) | 路由守卫（页面拦截） |
| 前端 | [api/request.js](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/frontend/src/api/request.js) | axios 实例（无请求拦截器） |
| 前端 | [App.vue](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/frontend/src/App.vue) | 顶栏用户名 / 退出登录 |
| 部署 | [nginx.conf](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/frontend/nginx.conf) | 前端静态资源 + `/api` 反向代理 |

---

## 二、从登录成功那一刻起，状态怎么一层层传下去

### 第 1 层：登录接口返回了什么（后端）

[AuthController.login()](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/backend/src/main/java/com/supermarket/controller/AuthController.java#L35-L51) 校验用户名/密码后，只返回一个普通 JSON：

```json
{ "id": 1, "username": "admin", "role": "ADMIN" }
```

关键点：
- **没有生成任何 token**（无 JWT、无自定义 token）。
- **没有写 Session / Cookie**（没用 `HttpSession`，`ResponseEntity` 里也没有 `Set-Cookie`）。
- 密码是**明文比对**（`user.getPassword().equals(loginUser.getPassword())`，见第 39 行），[DataInitializer.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/backend/src/main/java/com/supermarket/config/DataInitializer.java#L36-L48) 里初始账号是 `admin/123456`、`user/123456`，也是明文入库。

所以从后端角度看，“登录成功”只是**返回了一段用户资料**，服务端自己**不记住**任何人登录过。它是完全无状态的，但又不是 token 那种“无状态鉴权”，而是**根本没有鉴权**。

### 第 2 层：前端把返回值存进 localStorage

[Login.vue handleLogin()](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/frontend/src/views/Login.vue#L82-L94)：

```js
const data = await request.post('/auth/login', loginForm);
localStorage.setItem('user', JSON.stringify(data)); // 把 {id,username,role} 存本地
router.push('/products');
```

“登录状态”的**唯一载体**就是浏览器 `localStorage` 里那把叫 `user` 的键。它只是普通字符串，无签名、无有效期、无法验真伪。

### 第 3 层：每次打开页面——路由守卫拦截

[router/index.js beforeEach()](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/frontend/src/router/index.js#L43-L52)：

```js
const user = localStorage.getItem('user');
if (to.meta.requiresAuth && !user) {
    next('/login');          // 没有 user 就踢回登录页
} else if (to.path === '/login' && user) {
    next('/products');       // 已“登录”就别再看登录页
} else {
    next();
}
```

判断逻辑**只看 localStorage 里 `user` 这个键存不存在**，不看内容对不对、更不问后端。`/products`、`/categories`、`/suppliers` 三个页面挂了 `meta.requiresAuth`（见路由表 21/27/33 行），所以只有它们受这层守卫保护。

### 第 4 层：每次拉数据——请求发出去时带了什么

[api/request.js](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/frontend/src/api/request.js)：

```js
const instance = axios.create({ baseURL: '/api', timeout: 10000 });
// 只有【响应】拦截器，用来弹错误提示
instance.interceptors.response.use(response => response.data, error => { ... });
```

**这里是链路的断点**：axios 只配了**响应拦截器**，**没有请求拦截器**。也就是说，前端发商品/分类/供应商请求时，**根本不会把 localStorage 里的 `user` 附加到请求头或 Cookie 上**。业务请求（如 [ProductList.vue](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/frontend/src/views/ProductList.vue#L116-L129) 里的 `request.get('/products')`）是**裸请求**，不携带任何身份信息。

所以“登录状态”**根本没有传到后端**——它到第 3 层（前端路由）就停了。

### 第 5 层：顶栏显示与退出

[App.vue](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/frontend/src/App.vue#L76-L87) 里 `username` 也是直接读 `localStorage` 解析出来显示；退出登录就是 `localStorage.removeItem('user')` 再跳回登录页。整个生命周期都在浏览器本地闭环，**从不与后端交互验证**。

---

## 三、后端那些商品/分类/供应商接口，到底认不认这个状态

**结论：完全不认，也没法认。**

逐条证据：

1. **没有任何鉴权设施。** [pom.xml](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/backend/pom.xml#L19-L47) 依赖里只有 `web`、`data-jpa`、`validation`、`mysql`、`lombok`——**没有 `spring-boot-starter-security`**，也没有任何 JWT 库。全项目没有 `SecurityConfig`、没有拦截器（`HandlerInterceptor`）、没有过滤器（`Filter`）、没有 `@PreAuthorize`。

2. **业务 Controller 里没有任何身份判断。** 四个业务控制器的每个方法都是拿到请求就直接查库/写库：
   - 商品：`getAllProducts` / `createProduct` / `updateProduct` / `deleteProduct` 全部无校验（[ProductController.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/backend/src/main/java/com/supermarket/controller/ProductController.java)）。
   - 分类：`getAllCategories` / `createCategory` / `deleteCategory` 无校验。
   - 供应商：`getAllSuppliers` / 增删改 无校验。
   - 采购单：同样无校验。
   没有一个方法接收 `HttpServletRequest` 去读 header，也没有读 Session。

3. **CORS 全开。** 所有控制器（含 AuthController）都标了 `@CrossOrigin(origins = "*")`，任意来源的网页/脚本都能跨域调用这些接口。

4. **返回的 `role` 字段没人用。** 登录返回了 `role`（ADMIN/USER），但前端拦截只判断 `user` 存不存在，后端更没有基于角色的权限区分。所以“管理员 / 普通用户”在权限上没有任何实际差别。

---

## 四、什么情况下会绕过它出问题

因为“拦截”只发生在前端浏览器，而真正的数据在后端且后端不设防，以下场景都能绕过：

### 1. 直接调后端接口（最根本的问题）
不打开前端页面，用 `curl` / Postman / 浏览器地址栏 / 任意脚本，直接请求经 Nginx 代理（[nginx.conf](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/frontend/nginx.conf#L11-L17) 把 `/api` 转发到后端 8080）暴露出来的接口：

```bash
# 无需任何登录，直接读全部商品
curl http://<host>/api/products
# 无需登录，直接删除 id=1 的商品
curl -X DELETE http://<host>/api/products/1
# 无需登录，直接改供应商、增分类、造采购单……
curl -X POST http://<host>/api/categories -H "Content-Type: application/json" -d '{"name":"x"}'
```

全部会成功。前端那套 `requiresAuth` 守卫对这种调用**完全无效**，因为请求压根没经过前端路由。这是最严重的漏洞：**所有业务数据可被任意人匿名读、写、删。**

### 2. 手动伪造前端“登录态”
在浏览器控制台执行一句：

```js
localStorage.setItem('user', '{"username":"whoever"}');
```

刷新后路由守卫就认为“已登录”，可正常进入所有页面并操作。因为 `user` 内容从不被校验真伪。

### 3. 用户信息注册接口开放
[AuthController.register()](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/backend/src/main/java/com/supermarket/controller/AuthController.java#L20-L33) 也无任何限制，任何人可随意注册账号；且 `role` 字段来自请求体 `@RequestBody User`，理论上可自带 `"role":"ADMIN"` 注册成管理员（虽然当前 role 在后端没被用于鉴权，但一旦将来补鉴权，这里是提权隐患）。

### 4. localStorage 天生的暴露面
凭证放 `localStorage` 且是明文用户资料——任意页面内的 JS（如引入的第三方脚本、XSS）都能读到。虽然这里存的不是 token（token 本就不存在），但即便日后引入 token，用 localStorage 存放也易受 XSS 窃取。

---

## 五、链路总览图

```
[登录成功]
  后端 AuthController：校验明文密码 → 返回 {id,username,role}   ← 不发 token / 不写 Cookie / 不建 Session
       │
       ▼
  前端 Login.vue：localStorage.setItem('user', ...)            ← “登录状态”的唯一载体，纯本地、可伪造
       │
       ├──▶ 打开页面：router.beforeEach 读 localStorage.user   ← 只判断“存不存在”，不验真伪、不问后端
       │
       └──▶ 拉数据：axios 无请求拦截器                          ← ✗ 断点：user 不随请求发出
                     │
                     ▼
              后端业务 Controller（商品/分类/供应商/采购单）
                     └── 无 Security、无拦截器、无 role 校验     ← 裸请求直接读写库，谁来都认
```

**核心症结：** 前端建立了“登录状态”，但它在第 4 层（发请求）就丢失了，从未传给后端；而后端本身也**没有任何环节去接收和校验**这个状态。两边都没接上，等于后端对所有人敞开。前端的登录拦截只是“UI 层礼貌性引导”，不构成任何安全边界。

---

## 六、给后续整改的方向（仅提示，不在本次范围内改动）

- 后端引入真正的鉴权：登录成功签发 token（如 JWT）或建立 Session，返回给前端。
- 前端在 [request.js](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/frontend/src/api/request.js) 加**请求拦截器**，把 token 放进 `Authorization` 头随每次请求带上。
- 后端加统一的鉴权过滤器/拦截器或 Spring Security，对业务接口强制校验；按 `role` 做权限区分。
- 密码改为 BCrypt 加密存储（当前明文，AuthController 注释里也已提到）。
- 收敛 `@CrossOrigin(origins = "*")` 到可信来源。
- 注册接口不应允许客户端自带 `role`。

---

# 附录（第二轮补充，2026-08-01）

> 以下为在原分析基础上的深入补充。**上文第一～六节结论保持不动。**
> 补充分两块：(A) 后端全接口清单；(B) 伪造 user 进入页面的实际推演。
> 推演中若发现与前文结论有出入，见 **§B.4 与前文的对照修正**。

## A. 后端全接口清单（逐个过一遍）

前置事实（决定下表"身份校验/匿名可调"列的统一答案）：
- 全项目无 Spring Security、无 `Filter`/`HandlerInterceptor`、无 `@PreAuthorize`（见 [pom.xml](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/backend/pom.xml)）。
- 每个控制器都标注 `@CrossOrigin(origins = "*")`，任意来源可跨域调用。
- 因此**下表每一个接口的"身份校验"都是"无"，"匿名直接调"都是"能通"**。差别只在于"通了能干啥"。

涉及的数据结构（决定能读到/改到什么）：
- User：`id, username, password（明文）, role, createdAt`（[User.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/backend/src/main/java/com/supermarket/model/User.java)）
- Product：`id, name, barcode, price, stock, category`（[Product.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/backend/src/main/java/com/supermarket/model/Product.java)）
- Category：`id, name, description`（[Category.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/backend/src/main/java/com/supermarket/model/Category.java)）
- Supplier：`id, name, contact, phone, address`（[Supplier.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/backend/src/main/java/com/supermarket/model/Supplier.java)）
- PurchaseOrder：`id, supplier, product, quantity, unitPrice, totalAmount, orderDate, status`（[PurchaseOrder.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/backend/src/main/java/com/supermarket/model/PurchaseOrder.java)）

> 说明：路径 = `@RequestMapping` 类前缀 + 方法映射。Nginx 把 `/api` 转发到后端 8080（[nginx.conf](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/frontend/nginx.conf#L11-L17)），所以下列路径匿名对外可达。

### A.1 认证模块 —— [AuthController.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/backend/src/main/java/com/supermarket/controller/AuthController.java)

| 方法 | 路径 | 身份校验 | 匿名可调 | 通了能干啥 |
|---|---|---|---|---|
| POST | `/api/auth/register` | 无 | 能 | **注册任意账号**。请求体是整个 `User`，客户端可自带 `"role":"ADMIN"` 直接注册管理员（[register:20-33](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/backend/src/main/java/com/supermarket/controller/AuthController.java#L20-L33)）。仅校验用户名是否重复；密码明文入库。返回体已置空 password。 |
| POST | `/api/auth/login` | 无（这本身就是登录入口） | 能 | 明文比对密码；成功返回 `{id,username,role}`。**可被用作账号密码爆破/枚举接口**——无验证码、无锁定、无限速；用户名错与密码错返回同样的"用户名或密码错误"，但仍可无限尝试（[login:35-51](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/backend/src/main/java/com/supermarket/controller/AuthController.java#L35-L51)）。 |

注：无登出接口、无刷新接口、无"查当前用户"接口——因为后端根本不维护会话。

### A.2 商品模块 —— [ProductController.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/backend/src/main/java/com/supermarket/controller/ProductController.java)

| 方法 | 路径 | 身份校验 | 匿名可调 | 通了能干啥 |
|---|---|---|---|---|
| GET | `/api/products?name=` | 无 | 能 | **读全部商品**（名称、条码、价格、库存、所属分类）。带 `name` 走模糊查询，不带则全量返回。 |
| GET | `/api/products/category/{categoryId}` | 无 | 能 | 读某分类下全部商品。 |
| POST | `/api/products` | 无 | 能 | **新增商品**，字段任意可控。 |
| PUT | `/api/products/{id}` | 无 | 能 | **改任意商品**的名称/价格/库存/条码/分类。可篡改价格、刷库存。id 不存在会抛 `RuntimeException`（500）。 |
| DELETE | `/api/products/{id}` | 无 | 能 | **删任意商品**。`deleteById` 对不存在 id 静默返回 200（不报错）。 |

### A.3 分类模块 —— [CategoryController.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/backend/src/main/java/com/supermarket/controller/CategoryController.java)

| 方法 | 路径 | 身份校验 | 匿名可调 | 通了能干啥 |
|---|---|---|---|---|
| GET | `/api/categories` | 无 | 能 | **读全部分类**（名称、描述）。 |
| POST | `/api/categories` | 无 | 能 | **新增分类**。`name` 唯一约束，重名会 DB 异常（500）。 |
| DELETE | `/api/categories/{id}` | 无 | 能 | **删任意分类**。注意：Product 外键指向 Category，删被引用的分类可能触发外键约束错误（500），属数据完整性风险。 |

> 分类模块**没有 PUT/更新接口**（仅增、删、查）。

### A.4 供应商模块 —— [SupplierController.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/backend/src/main/java/com/supermarket/controller/SupplierController.java)

| 方法 | 路径 | 身份校验 | 匿名可调 | 通了能干啥 |
|---|---|---|---|---|
| GET | `/api/suppliers` | 无 | 能 | **读全部供应商**，含**联系人姓名、电话、地址等 PII**（如初始数据"张经理/13800138001"）。 |
| POST | `/api/suppliers` | 无 | 能 | **新增供应商**。 |
| PUT | `/api/suppliers/{id}` | 无 | 能 | **改任意供应商**的名称/联系人/电话/地址。id 不存在抛 `RuntimeException`（500）。 |
| DELETE | `/api/suppliers/{id}` | 无 | 能 | **删任意供应商**。 |

### A.5 采购单模块 —— [PurchaseOrderController.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/backend/src/main/java/com/supermarket/controller/PurchaseOrderController.java)

| 方法 | 路径 | 身份校验 | 匿名可调 | 通了能干啥 |
|---|---|---|---|---|
| GET | `/api/orders?status=` | 无 | 能 | **读全部采购单**（供应商、商品、数量、单价、总额、下单时间、状态），可含商业敏感的采购价与往来。带 `status` 过滤。 |
| POST | `/api/orders` | 无 | 能 | **新建采购单**，金额/数量/状态全可控。 |
| PUT | `/api/orders/{id}` | 无 | 能 | **改任意采购单**，含 `totalAmount`、`status`——可伪造/篡改采购金额与单据状态。id 不存在抛 `RuntimeException`（500）。 |
| DELETE | `/api/orders/{id}` | 无 | 能 | **删任意采购单**。 |

> 补充盲点：采购单接口在前端没有任何页面调用（[router](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/frontend/src/router/index.js) 里只有商品/分类/供应商三个受保护页面），属于**只有后端、前端未暴露**的接口。它同样零防护，是容易被忽视的攻击面。

### A.6 清单小结

- 后端共 **16 个接口**（认证 2 + 商品 5 + 分类 3 + 供应商 4 + 采购单 4，其中商品的两个 GET 合并算作查询能力）。
- **身份校验：16/16 全部为"无"。**
- **匿名直接调：16/16 全部"能通"。**
- 读能力：可拉走全部商品、分类、供应商（含 PII）、采购单（含金额）。
- 写能力：可任意增改删上述四类业务数据；可注册任意账号（含 ADMIN 角色）。
- 唯一"报错"差异：`updateXxx` 对不存在 id 抛 500，`deleteById` 对不存在 id 静默 200——但都不构成任何访问控制。

## B. 伪造 user 进入页面的实际推演

推演前提：攻击者在浏览器控制台执行一句（对照 §四.2）：

```js
localStorage.setItem('user', '{"username":"whoever"}');
```

然后访问 `/products`。下面逐点推演页面表现。

### B.1 能顺利进去（无感）

- **路由放行**：[beforeEach](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/frontend/src/router/index.js#L43-L52) 只判断 `localStorage.getItem('user')` 是否为真值，不解析、不验签、不问后端。伪造的字符串存在即放行，三张受保护页面（商品/分类/供应商）**全部可进**，完全无感。
- **业务数据照常加载**：进入 [ProductList.vue](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/frontend/src/views/ProductList.vue#L194-L197) 后 `onMounted` 调 `/products`、`/categories`，因后端零校验，**数据正常返回并渲染**。增删改商品也都能成功。这里**完全无感**——因为真实数据本就不需要登录态。

### B.2 会露馅的地方

- **顶栏用户名显示**：[App.vue 的 username 计算属性](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/frontend/src/App.vue#L76-L82) 做的是 `JSON.parse(user).username`。
  - 若伪造串带了 `username`（如上例 `whoever`），顶栏就显示 **`whoever`**——不会露馅，攻击者还能自定义显示名。
  - 若伪造串**不含 username**（如 `localStorage.setItem('user','{}')` 或 `'1'`），`JSON.parse` 成功但 `.username` 为 `undefined`，顶栏用户名处**显示为空白**（只剩一个用户图标 `<user-outlined/>` 后面没有文字）。**这是最直接的露馅点**。
  - 若伪造串**不是合法 JSON**（如 `localStorage.setItem('user','abc')`），`JSON.parse` 抛异常。该 computed 无 try/catch，会在渲染期报错——顶栏渲染异常/控制台报错，**明显露馅**（详见 §B.4，这与"随便塞个 user 就能无感进入"需要区分）。

### B.3 点"退出登录"会怎样

[handleLogout](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/frontend/src/App.vue#L84-L87)：

```js
const handleLogout = () => {
  localStorage.removeItem('user');
  router.push('/login');
};
```

- 纯前端操作：删掉本地 `user`、跳到 `/login`。**不调用任何后端接口**（后端也没有登出接口）。
- 对伪造用户而言，效果和真实用户**完全一样**：本地键被清、被送回登录页。没有任何"这是假账号"的识别或告警——**这一步无感、无差别**。
- 退出后再想进受保护页面，又会被路由守卫挡回登录页；但只要再塞一次假 `user` 即可重新进入。

### B.4 与前文结论的对照修正

第一轮 §四.2 的原话是"在浏览器控制台执行 `localStorage.setItem('user', '{"username":"whoever"}')`，刷新后路由守卫就认为已登录"。本轮深入推演后，对该结论做**如下细化（不是推翻，是补充边界条件）**：

1. **"随便塞个 user 就能无感进入"需加限定**：只有当伪造值是**合法 JSON** 时才真正无感。
   - 合法 JSON 且含 `username` → 完全无感，顶栏还显示伪造名。
   - 合法 JSON 但缺 `username`（如 `{}`）→ 能进页面，但**顶栏用户名空白**，属轻微露馅。
   - **非法 JSON**（如 `'abc'`）→ 路由守卫仍放行（它只判断真值），但 [App.vue username computed](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/frontend/src/App.vue#L76-L82) 的 `JSON.parse` 会抛错导致顶栏渲染异常。**即"能通过路由守卫"和"页面能正常显示"是两件事**——前者门槛极低（真值即可），后者要求合法 JSON。这一层区分在第一轮未展开。

2. 结论方向不变：无论哪种情况，**路由守卫都拦不住伪造**，后端数据照样能拿。露馅与否只影响"骗过肉眼观察者"的完美程度，不影响越权本身是否成立。

> 综合看：伪造 user 的"最佳实践"（从攻击者角度）是塞一个含 `username` 的合法 JSON，此时前端从进入到操作到退出**全程无感、无破绽**；只有粗糙的伪造值才会在顶栏用户名处露馅或引发渲染报错。

---

# 附录（第三轮补充，2026-08-01）：鉴权补全方案

> 本节只出方案、不动代码。前两轮及第一～六节结论保持不动。
> 目标：把"登录状态"从纯前端假象，变成**后端强制、每次请求可验证**的真实鉴权。
> 结构：§C 路线取舍 → §D 逐文件/逐接口改动 → §E 附带问题 → §F 改动先后（堵漏顺序）。

## C. token 还是 session：这套系统怎么选

先摆事实，再给结论。

| 维度 | Session（服务端会话 + Cookie） | Token（JWT，`Authorization: Bearer`） |
|---|---|---|
| 后端现状契合度 | 需要引入会话存储（内存/Redis）；Spring 有现成 `HttpSession` | 无状态，不依赖存储，签发/校验各一段代码 |
| 前端改动量 | Cookie 由浏览器自动带，`request.js` 几乎不用动请求头 | 需在 `request.js` 请求拦截器里手动加头 |
| 与当前部署契合 | 单体后端 + Nginx 同源代理（[nginx.conf](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/frontend/nginx.conf)），同源下 Cookie 天然可用 | 同样适用；跨域时更省心 |
| CSRF | Cookie 自动携带 → **需额外防 CSRF** | Bearer 头需 JS 主动加 → **天然免疫 CSRF** |
| 主动登出/踢人 | 服务端删会话即可，强 | JWT 无状态，需黑名单/短有效期才能撤销 |
| 与现有前端存储习惯 | 改为 HttpOnly Cookie（更安全，JS 读不到） | 现在就是 `localStorage` 存 user，改存 token 顺手 |

**结论：本系统选 Token（JWT），但把"当前用户信息"和"凭证"分开处理。**

理由：
1. 后端是单体、无状态最省事，不必为这么小的系统引入 Redis 或依赖 JVM 内存会话（重启即失效、不利于水平扩展）。
2. 前端现在已经是"登录后往 `localStorage` 存东西"的模式，改成存 token 的改动面最小、最自然（见 §D）。
3. 这套系统当前 `@CrossOrigin(origins="*")` 全开、且没有任何 CSRF 防护，Bearer token 天然免疫 CSRF，能顺带回避一个坑。
4. 唯一要补的短板是"主动撤销"——用**短有效期（如 2 小时）**即可覆盖绝大多数场景；本系统暂无"强制踢人"需求，不值得为此上黑名单。若日后需要，再加 Redis 黑名单不迟。

> 如果团队更看重"能随时踢人 + 不想碰 JWT 库"，Session 也是合理选择，方案里 §D 的后端过滤器换成 `HttpSession` 校验即可，前端 `request.js` 反而更简单（去掉加头逻辑）。但本方案默认走 JWT。

## D. 逐文件、逐接口：动什么、为什么

### D.1 后端：新增依赖与配置

- **[pom.xml](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/backend/pom.xml)**
  - 加 `spring-boot-starter-security`（提供密码加密器、过滤器链骨架）。
  - 加 JWT 库（如 `io.jsonwebtoken:jjwt`）。
  - **为什么**：当前依赖里完全没有安全设施（前两轮已确认），这是一切的前提。

- **新增 `application.properties` 配置项**（[application.properties](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/backend/src/main/resources/application.properties)）
  - JWT 签名密钥（从环境变量注入，**不要硬编码进仓库**）、token 有效期。
  - **为什么**：密钥进代码库等于没有密钥。

### D.2 后端：新增 3 个类（安全骨架）

1. **`SecurityConfig`（新文件，`config` 包）**
   - 定义 `SecurityFilterChain`：`/api/auth/login`、`/api/auth/register` 放行；**其余 `/api/**` 一律要求已认证**。
   - `SessionCreationPolicy.STATELESS`（走 JWT，不建会话）。
   - 注册 `BCryptPasswordEncoder` Bean。
   - 配置 CORS（见 §E.3，收敛白名单）。
   - **为什么**：这是把"默认全放行"翻转为"默认全拦截、白名单放行"的唯一开关。有了它，前两轮清单里 16 个接口中的 14 个业务接口一次性从"匿名可调"变成"必须带有效 token"。

2. **`JwtUtil`（新文件）**：签发、解析、校验 token（含过期判断）。

3. **`JwtAuthenticationFilter`（新文件，继承 `OncePerRequestFilter`）**
   - 从 `Authorization: Bearer xxx` 取 token → 校验 → 放进 `SecurityContext`。
   - **为什么**：这是"每次请求都验证登录状态"的落点——正好补上第一轮 §二"第 4 层断点"（请求不带身份）在后端的接收端。

### D.3 后端：登录/注册接口改造 —— [AuthController.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/backend/src/main/java/com/supermarket/controller/AuthController.java)

- **`login`（[35-51](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/backend/src/main/java/com/supermarket/controller/AuthController.java#L35-L51)）**：
  - 密码比对改为 `passwordEncoder.matches(raw, stored)`（不再 `.equals` 明文）。
  - 校验通过后**签发 JWT**（payload 放 `username`、`role`），返回 `{token, username, role}`。
  - **为什么**：这是"登录成功那一刻"真正下发凭证的地方——直接修掉第一轮的核心症结"后端登录不发任何凭证"。
- **`register`（[20-33](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/backend/src/main/java/com/supermarket/controller/AuthController.java#L20-L33)）**：
  - 存库前 `password = encoder.encode(password)`。
  - **不接受客户端传入的 `role`**：服务端强制写死 `USER`（见 §E.2）。
  - **为什么**：堵住第二轮清单 A.1 里"自带 role:ADMIN 注册管理员"的提权点。

### D.4 后端：四个业务 Controller —— 原则上"不改代码，靠 SecurityConfig 兜底"

- [ProductController](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/backend/src/main/java/com/supermarket/controller/ProductController.java)、[CategoryController](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/backend/src/main/java/com/supermarket/controller/CategoryController.java)、[SupplierController](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/backend/src/main/java/com/supermarket/controller/SupplierController.java)、[PurchaseOrderController](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/backend/src/main/java/com/supermarket/controller/PurchaseOrderController.java)：
  - **认证层面无需逐方法加代码**——`SecurityConfig` 里 `/api/**` 默认需认证后，这 14 个接口自动全部受保护。这是选"统一过滤器"而非"每个方法手写校验"的关键好处。
  - **若要做角色区分**（例如：写操作限 ADMIN、读操作 USER 即可），则在需要的方法上加 `@PreAuthorize("hasRole('ADMIN')")`。对照前两轮清单，**建议保护的写接口**：
    - 商品：`POST/PUT/DELETE /api/products/**`
    - 分类：`POST/DELETE /api/categories/**`
    - 供应商：`POST/PUT/DELETE /api/suppliers/**`
    - 采购单：`POST/PUT/DELETE /api/orders/**`（尤其 `PUT` 能改 `totalAmount`/`status`，敏感）
  - 去掉每个类上的 `@CrossOrigin(origins="*")`，改由 `SecurityConfig` 统一配 CORS（见 §E.3）。
  - **为什么**：控制器保持"薄"、不散落鉴权逻辑，权限规则集中在一处，易审计、不易漏。

### D.5 前端：请求侧 —— [api/request.js](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/frontend/src/api/request.js)

- **新增请求拦截器**：每次请求前从存储读 token，塞进 `Authorization: Bearer <token>`。
- **扩展响应拦截器**：收到 `401` 时清掉本地凭证并跳 `/login`（token 过期/无效的统一出口）。
- **为什么**：这正是第一轮 §二"第 4 层断点"的修复点——让登录状态**真正随请求传到后端**。这是整个链路里最关键的一处前端改动，缺了它后端做得再好也连不上。

### D.6 前端：登录页 —— [Login.vue](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/frontend/src/views/Login.vue)

- `handleLogin`（[82-94](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/frontend/src/views/Login.vue#L82-L94)）：把后端返回的 `token` 存起来（键名如 `token`），用户展示信息（username/role）可另存。
- **为什么**：凭证与展示信息分离，路由守卫改判 token（见 D.7），使"塞个假 user 就能进"失效——因为假 token 会被后端拒（401）。

### D.7 前端：路由守卫 —— [router/index.js](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/frontend/src/router/index.js)

- `beforeEach`（[43-52](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/frontend/src/router/index.js#L43-L52)）：判断依据从 `localStorage.user` 改为 `localStorage.token`。
- **要点**：前端守卫**仍只是体验层**（决定是否跳登录页），**真正的安全边界在后端**。伪造 token 仍能骗过守卫进入页面，但一旦发请求就被后端 401 拦下、数据拿不到。这一点必须写清，避免重蹈"以为前端拦截=安全"的覆辙。
- **为什么**：对齐凭证载体；同时明确前端守卫不再被当作安全手段。

### D.8 前端：顶栏与登出 —— [App.vue](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/frontend/src/App.vue)

- `username` computed（[76-82](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/frontend/src/App.vue#L76-L82)）：读取用户展示信息处**加 try/catch**，避免第二轮 §B.2 里"非法 JSON 导致顶栏渲染报错"。
- `handleLogout`（[84-87](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/frontend/src/App.vue#L84-L87)）：同时清 token 与展示信息。
- **为什么**：健壮性收尾，顺带修掉第二轮发现的渲染脆弱点。

## E. 附带问题：一起处理

三个都建议**一起处理**，因为它们和鉴权是同一批文件、同一次改动窗口，分开做反而重复劳动。

### E.1 明文密码（必须处理，优先级高）
- 现状：[AuthController](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/backend/src/main/java/com/supermarket/controller/AuthController.java#L39) `.equals` 明文比对，[DataInitializer](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/backend/src/main/java/com/supermarket/config/DataInitializer.java#L36-L48) 明文初始化 `admin/123456`、`user/123456`。
- 方案：引入 `BCryptPasswordEncoder`；登录改 `matches`、注册改 `encode`；**[DataInitializer](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/backend/src/main/java/com/supermarket/config/DataInitializer.java) 里初始账号密码也要改成 `encode` 后再存**，否则新登录逻辑用 `matches` 比对明文库数据会全部失败（这是最容易漏的联动点）。
- 与鉴权同批做，因为 encoder Bean 本来就要在 `SecurityConfig` 里定义。

### E.2 注册能自带 role（必须处理）
- 现状：`register` 收整个 `User`（[20-33](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/backend/src/main/java/com/supermarket/controller/AuthController.java#L20-L33)），可带 `role:ADMIN`。当前 role 未用于鉴权所以"看似无害"，但一旦 §D.4 上了 `@PreAuthorize("hasRole('ADMIN')")`，这个口子立刻变成**任意人自助提权为管理员**。
- 方案：注册接口服务端强制 `role="USER"`；管理员只能由已有管理员另行赋权或初始化时写入。
- **务必和鉴权同批上线**，否则会出现"刚加了角色控制、却留着自助注册 ADMIN 后门"的更糟状态。

### E.3 CORS 全开（建议处理，优先级中）
- 现状：所有控制器 `@CrossOrigin(origins="*")`。
- 方案：移除各控制器上的注解，在 `SecurityConfig` 统一配置允许的来源（生产域名 / 本地开发地址白名单）。
- 说明：本系统前端经 Nginx 与后端**同源**（[nginx.conf](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/frontend/nginx.conf) 把 `/api` 反代到后端），正常访问其实**不需要** CORS 全开；`*` 纯属放大攻击面。选 JWT + Bearer 后无 Cookie 自动携带，收敛 CORS 主要是减少被跨站脚本滥用的余地。

## F. 改动先后：哪些必须先堵

排序原则：**先让后端具备"能拒绝"的能力，再接前端；先堵能改/删数据的写接口，再堵读接口。** 对照前两轮 16 个接口清单：

**第 0 步（地基，必须最先）**
- pom 加依赖 + `SecurityConfig` + `JwtUtil` + `JwtAuthenticationFilter` + encoder Bean（§D.1/D.2）。
- 同时改 [DataInitializer](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-tony/backend/src/main/java/com/supermarket/config/DataInitializer.java) 与登录/注册（§D.3、§E.1、§E.2）——否则登录本身会崩，后面没法测。
- **完成即效果**：`/api/**` 默认需认证，前两轮清单里 14 个业务接口**一次性全部从"匿名可调"变为"需 token"**。这是收益最大的一步。

**第 1 步（最高危写接口，即使还没做角色区分也应最先验证已生效）**
按"改了/删了后果最重"排序：
1. 采购单 `PUT /api/orders/{id}`、`DELETE /api/orders/{id}`（能篡改金额/状态、删单据）。
2. 商品 `PUT /api/products/{id}`、`DELETE /api/products/{id}`（改价、刷库存、删品）。
3. 供应商 `PUT/DELETE /api/suppliers/**`、分类 `DELETE /api/categories/{id}`（删被引用分类还会连带数据完整性问题）。
4. 各 `POST`（新增类）。
- 这些在第 0 步后已被 token 门槛覆盖；此步是**加角色区分**（`@PreAuthorize`）并逐个验证。

**第 2 步（读接口）**
- 商品/分类/供应商/采购单的 `GET`。危害低于写接口，但供应商 GET 含 PII、采购单 GET 含金额，仍需登录后可读。第 0 步其实已一并保护，此步主要是确认无遗漏、无被 `permitAll` 误放行。

**第 3 步（前端接线）**
- `request.js` 请求拦截器加 Bearer 头 + 401 处理（§D.5）→ 这是让合法用户重新能用的关键。
- 登录页存 token、路由守卫改判 token、App.vue 加固（§D.6/D.7/D.8）。
- **注意顺序**：前端接线要在后端第 0 步上线后紧跟，否则中间窗口期合法用户会因为"后端要 token、前端还没带"而全部 401。理想是后端与前端**同批发布**。

**第 4 步（收尾）**
- 收敛 CORS（§E.3）。
- 复核注册接口确实不再接受外部 role（§E.2）——作为上线前最后一道确认。

> 一句话总结先后：**先上后端安全地基（含改初始化数据，否则登录崩），业务接口即被一次性兜底；随后端同批把前端 request.js/路由/登录页接上 token；最后收敛 CORS 并复核注册提权口。** 最该先堵的单点是采购单与商品的 `PUT/DELETE`——它们是"匿名就能改钱、改库存、删数据"的最高危面。
