# 登录状态链路分析

> 本文只做分析，不改任何代码。所有结论以仓库当前实际实现为准。
> 一句话结论：**这套系统的"登录状态"完全是前端在浏览器里自娱自乐，后端没有任何凭证、没有任何校验。所谓的"登录拦截"只是前端路由的一道视觉门禁，对后端接口没有任何约束力。**

---

## 一、先给结论（TL;DR）

1. 登录成功后，后端**不发任何凭证**（没有 Token、没有 Session、没有 Cookie），只回了一段用户信息 JSON。
2. 前端把这段 JSON 原样塞进 `localStorage` 的 `user` 键，"登录状态"从此就是"`localStorage` 里有没有 `user` 这个键"。
3. 每次打开页面，前端路由守卫只判断 `localStorage.getItem('user')` 是否存在，存在就放行。
4. 每次拉数据（商品/分类/供应商），前端请求**不带任何身份信息**，后端接口也**完全不校验身份**。
5. 因此后端所有业务接口都是**匿名全开放**的：任何人不登录、直接调接口，就能增删改查所有数据。前端的登录拦截**可以被轻易绕过**。

---

## 二、从"登录成功那一刻"开始，逐层看链路

### 第 1 层：后端登录接口返回了什么

文件：[AuthController.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-thor/backend/src/main/java/com/supermarket/controller/AuthController.java#L35-L51)

```java
@PostMapping("/login")
public ResponseEntity<?> login(@RequestBody User loginUser) {
    User user = userRepository.findByUsername(loginUser.getUsername());
    if (user == null || !user.getPassword().equals(loginUser.getPassword())) {
        // 用户名或密码错误
        return ResponseEntity.badRequest().body(error);
    }
    Map<String, Object> response = new HashMap<>();
    response.put("id", user.getId());
    response.put("username", user.getUsername());
    response.put("role", user.getRole());
    return ResponseEntity.ok(response);
}
```

**关键观察：**
- 校验方式是明文密码字符串直接 `equals` 比较（`user.getPassword().equals(...)`），密码在库里也是明文存储（见 [DataInitializer.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-thor/backend/src/main/java/com/supermarket/config/DataInitializer.java#L36-L48)，`admin/123456`、`user/123456`）。
- 登录成功后返回的只是 `id / username / role` 三个字段的 JSON。
- **没有 `Set-Cookie`、没有 JWT、没有 session id、没有任何服务端会话记录。** 服务端做完这次比对就"忘了"你，它根本不记得谁登录过。

> 所以你观察到的"后端没给前端发凭证"是准确的——它确实没发，因为这套后端压根没有会话/凭证机制。

### 第 2 层：前端拿到返回后怎么"记住"登录

文件：[Login.vue](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-thor/frontend/src/views/Login.vue#L82-L94)

```js
const handleLogin = async () => {
  const data = await request.post('/auth/login', loginForm);
  localStorage.setItem('user', JSON.stringify(data)); // 把 {id,username,role} 存进 localStorage
  message.success('登录成功');
  router.push('/products');
};
```

**"登录状态"的载体就是这一行 `localStorage.setItem('user', ...)`。** 从此以后，全前端判断"登录了没"，就看 `localStorage` 里有没有 `user` 这个键、能不能 `JSON.parse` 出东西。

- 它存在浏览器本地，不会随请求自动发给后端（`localStorage` 不像 Cookie 会自动携带）。
- 它没有有效期，不会过期，除非手动清或调用登出。

### 第 3 层：每次打开页面，路由守卫怎么拦

文件：[router/index.js](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-thor/frontend/src/router/index.js#L43-L52)

```js
router.beforeEach((to, from, next) => {
    const user = localStorage.getItem('user');
    if (to.meta.requiresAuth && !user) {
        next('/login');               // 没登录 -> 踢回登录页
    } else if (to.path === '/login' && user) {
        next('/products');            // 已登录还去登录页 -> 送回首页
    } else {
        next();
    }
});
```

`/products`、`/categories`、`/suppliers` 三个页面都带了 `meta: { requiresAuth: true }`，守卫的判据**仅仅是 `localStorage` 里 `user` 键是否存在**，不校验内容真伪、不问后端。

- 顶栏用户名展示、登出，也都是读/删这个键（[App.vue](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-thor/frontend/src/App.vue#L76-L87)）：登出就是 `localStorage.removeItem('user')` 然后跳登录页。

> 这就是你说的"每个页面都做了没登录就跳回登录页的拦截"——它是**纯前端、纯客户端**的判断，跟后端毫无关系。

### 第 4 层：每次拉数据，请求里带了什么

文件：[api/request.js](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-thor/frontend/src/api/request.js)

```js
const instance = axios.create({
    baseURL: '/api',
    timeout: 10000
});

// 只有响应拦截器，用于错误提示
instance.interceptors.response.use(
    response => response.data,
    error => {
        const msg = error.response?.data?.message || '网络请求失败';
        message.error(msg);
        return Promise.reject(error);
    }
);
```

**关键观察：**
- 这里**只有响应拦截器**（做错误弹窗），**没有请求拦截器**。
- 也就是说，发往后端的每个请求**不会附带 `Authorization` 头、不会带 `user`、不会带任何身份标识**。
- `localStorage` 里的 `user` 从头到尾**没有进入过任何一次请求**。它只服务于前端路由判断，从不发给后端。

### 第 5 层：后端业务接口认不认这个状态

商品、分类、供应商三个控制器，均无任何鉴权：

- [ProductController.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-thor/backend/src/main/java/com/supermarket/controller/ProductController.java)
- [CategoryController.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-thor/backend/src/main/java/com/supermarket/controller/CategoryController.java)
- [SupplierController.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-thor/backend/src/main/java/com/supermarket/controller/SupplierController.java)

以 ProductController 为例，`@GetMapping / @PostMapping / @PutMapping / @DeleteMapping` 直接读参数、直接操作数据库，**没有任何 `@PreAuthorize`、没有拦截器、没有过滤器、没有从请求里取身份的动作**：

```java
@DeleteMapping("/{id}")
public ResponseEntity<?> deleteProduct(@PathVariable Long id) {
    productRepository.deleteById(id);   // 谁都能删，无需登录
    return ResponseEntity.ok().build();
}
```

**工程级佐证：**
- `pom.xml` 里**没有 `spring-boot-starter-security` 依赖**（只有 web / data-jpa / validation / test）。所以整个应用没有 Spring Security 过滤链，默认所有接口开放。
- 每个控制器都加了 `@CrossOrigin(origins = "*")`，跨域完全放开。
- `role`（USER/ADMIN）字段登录时返回了，但**前后端都没有基于它做任何权限区分**——它是"死数据"，没有任何拦截作用。

**所以对第 4 层的问题的回答是：后端根本不认这个"登录状态"，因为它既收不到，也从不检查。**

---

## 三、整条链路串起来（时序图）

```
登录时：
  浏览器 --POST /api/auth/login {username,password}--> 后端
  后端明文比对密码 --200 {id,username,role}--> 浏览器
  浏览器 localStorage.setItem('user', JSON)       ← “登录状态”仅存于此

打开页面时：
  vue-router beforeEach 读 localStorage['user']
    存在 -> 放行；不存在 -> 跳 /login          ← 纯前端判断，后端不参与

拉数据时：
  浏览器 --GET /api/products （无任何身份头）--> nginx --> 后端
  后端 不校验任何身份，直接查库返回              ← 完全匿名开放
```

---

## 四、什么情况下会"绕过"它出问题

这里的核心风险是：**前端拦截是"演示性"的，后端才是真正的数据闸门，而后端这道闸门是敞开的。** 任何不经过前端界面的调用都能绕过。

1. **直接调后端接口（最典型）**
   不打开前端页面，直接用 `curl` / Postman / 浏览器地址栏访问后端，就能读写全部数据，无需任何登录：
   ```
   curl http://<host>:8080/api/products                 # 读全部商品
   curl -X DELETE http://<host>:8080/api/products/1      # 删除商品，无需登录
   curl -X POST  http://<host>:8080/api/suppliers -H "Content-Type: application/json" -d '{...}'
   ```
   分类、供应商、采购单接口同理。**这是最大的问题：整套业务数据对未认证的人完全暴露（可增删改查）。**

2. **手动伪造前端登录态**
   在浏览器控制台执行一句 `localStorage.setItem('user','{"username":"x","role":"ADMIN"}')`，前端路由守卫立刻认为你已登录，并放行所有页面——**无需真实账号密码**。因为守卫只看键是否存在，不验真伪、不问后端。role 还能随便写成 ADMIN。

3. **登录态永不失效**
   `localStorage` 中的 `user` 没有有效期、后端也没有会话可作废。只要不手动登出/清缓存，这个"登录"永远有效；即使后台把该用户删了或改了密码，前端照样畅通无阻（因为后端从不复核）。

4. **CORS 全放开放大了风险**
   `@CrossOrigin(origins = "*")` 意味着任意第三方网页的 JS 都能跨域直接打这些无鉴权接口。

5. **附带的敏感问题（虽非本次主线，但同源于"无鉴权"）**
   - 密码明文存储、明文比对（[AuthController.java:39](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-thor/backend/src/main/java/com/supermarket/controller/AuthController.java#L39)、[DataInitializer.java:39](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-thor/backend/src/main/java/com/supermarket/config/DataInitializer.java#L39)）。
   - `/api/auth/register` 也无限制，任何人可自助注册账号（虽然登不登录都不影响调业务接口）。

---

## 五、一句话总结

- **登录状态怎么传下去的？** 它没有"传下去"。登录成功后信息只落在浏览器 `localStorage['user']`，仅供前端路由守卫本地判断是否放行页面；请求发给后端时不携带它，后端也不记录会话。
- **后端认不认？** 完全不认。商品/分类/供应商接口没有任何鉴权（无 Spring Security、无拦截器、无 Token 校验），是匿名全开放的。
- **什么情况下绕过出问题？** 只要不走前端界面（直接调接口），或手动往 `localStorage` 塞个 `user`，就能绕过所有"登录拦截"，全量读写后端数据。前端的登录门禁本质上只是 UI 层面的摆设，不具备安全约束力。

---
---

# 补充分析（第二轮）

> 以下两节为在上一轮结论基础上的深入补充。**上面第一~五节的原始结论一律未改动。** 推演过程中如发现与前述结论有出入之处，统一在文末【与前轮结论的对照】中单独标出。

## 六、后端接口完整清单（逐个过一遍）

范围覆盖全部 5 个控制器：认证、商品、分类、供应商、采购单。经核对，**没有任何接口带鉴权**——全项目无 Spring Security 依赖、无 `Filter`/`Interceptor`/`@PreAuthorize`、无从请求读取身份的代码。因此下表"身份校验"一列全部为"无"，"匿名直接调能否通"全部为"能"。

数据模型参考：
- User：`id / username / password / role / createdAt`（[User.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-thor/backend/src/main/java/com/supermarket/model/User.java)）
- PurchaseOrder：`id / supplier / product / quantity / unitPrice / totalAmount / orderDate / status`（[PurchaseOrder.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-thor/backend/src/main/java/com/supermarket/model/PurchaseOrder.java)）

### 6.1 认证接口 —— [AuthController.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-thor/backend/src/main/java/com/supermarket/controller/AuthController.java)（`/api/auth`）

| # | 方法 | 路径 | 身份校验 | 匿名可通 | 通了能干啥 |
|---|------|------|----------|----------|------------|
| 1 | POST | `/api/auth/register` | 无 | 能 | 任何人自助注册新账号；仅校验用户名是否重复，`role` 由请求体决定，**可直接注册出一个 ADMIN 账号**。返回体已清空 password。 |
| 2 | POST | `/api/auth/login` | 无（本身就是登录入口） | 能 | 明文比对密码；成功返回 `{id, username, role}`。失败返回 400。无锁定/限流，**可无限撞库**。 |

### 6.2 商品接口 —— [ProductController.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-thor/backend/src/main/java/com/supermarket/controller/ProductController.java)（`/api/products`）

| # | 方法 | 路径 | 身份校验 | 匿名可通 | 通了能干啥 |
|---|------|------|----------|----------|------------|
| 3 | GET | `/api/products?name=` | 无 | 能 | 读取全部商品（可按名称模糊查）：名称、价格、库存、条码、所属分类。 |
| 4 | GET | `/api/products/category/{categoryId}` | 无 | 能 | 读取指定分类下全部商品。 |
| 5 | POST | `/api/products` | 无 | 能 | 新建商品，字段任意可控（可造脏数据、负价/负库存无校验）。 |
| 6 | PUT | `/api/products/{id}` | 无 | 能 | 改掉任意商品的名称/价格/库存/条码/分类（如把价格改成 0）。id 不存在会抛 `RuntimeException`→500。 |
| 7 | DELETE | `/api/products/{id}` | 无 | 能 | 删除任意商品。`deleteById` 对不存在 id 静默返回 200。 |

### 6.3 分类接口 —— [CategoryController.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-thor/backend/src/main/java/com/supermarket/controller/CategoryController.java)（`/api/categories`）

| # | 方法 | 路径 | 身份校验 | 匿名可通 | 通了能干啥 |
|---|------|------|----------|----------|------------|
| 8 | GET | `/api/categories` | 无 | 能 | 读取全部分类（名称、描述）。 |
| 9 | POST | `/api/categories` | 无 | 能 | 新建分类。 |
| 10 | DELETE | `/api/categories/{id}` | 无 | 能 | 删除任意分类。**注意**：无更新接口，且删除不检查是否有商品挂在该分类下（详见对照节的风险提示）。 |

### 6.4 供应商接口 —— [SupplierController.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-thor/backend/src/main/java/com/supermarket/controller/SupplierController.java)（`/api/suppliers`）

| # | 方法 | 路径 | 身份校验 | 匿名可通 | 通了能干啥 |
|---|------|------|----------|----------|------------|
| 11 | GET | `/api/suppliers` | 无 | 能 | 读取全部供应商：名称、联系人、电话、地址（**含联系人手机号等 PII**）。 |
| 12 | POST | `/api/suppliers` | 无 | 能 | 新建供应商。 |
| 13 | PUT | `/api/suppliers/{id}` | 无 | 能 | 改掉任意供应商的名称/联系人/电话/地址。id 不存在→500。 |
| 14 | DELETE | `/api/suppliers/{id}` | 无 | 能 | 删除任意供应商。 |

### 6.5 采购单接口 —— [PurchaseOrderController.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-thor/backend/src/main/java/com/supermarket/controller/PurchaseOrderController.java)（`/api/orders`）

| # | 方法 | 路径 | 身份校验 | 匿名可通 | 通了能干啥 |
|---|------|------|----------|----------|------------|
| 15 | GET | `/api/orders?status=` | 无 | 能 | 读取全部采购单（可按状态过滤）：供应商、商品、数量、单价、总额、下单时间、状态。**这是全套经营数据（采购成本、供货关系）。** |
| 16 | POST | `/api/orders` | 无 | 能 | 新建采购单，金额/数量任意可控。 |
| 17 | PUT | `/api/orders/{id}` | 无 | 能 | 改掉任意采购单，包括把 `status` 改成 `COMPLETED`/`CANCELLED`、篡改金额。id 不存在→500。 |
| 18 | DELETE | `/api/orders/{id}` | 无 | 能 | 删除任意采购单。 |

**清单小结：** 共 18 个接口，**0 个有身份校验，18 个匿名可通**。危害面覆盖：读全量业务数据 + PII（供应商联系方式）+ 经营数据（采购价/供货关系）；写侧可任意增删改所有业务实体，并能自助注册管理员账号。所有接口叠加 `@CrossOrigin(origins = "*")`，任意第三方站点 JS 均可跨域直接发起上述调用。

> 补充说明：采购单接口（`/api/orders`）在前端 `views` 里**没有对应页面**，属于"前端未使用但后端已暴露"的接口——即便把前端拦截做扎实，这类接口也只能靠后端鉴权来兜，进一步说明鉴权必须落在后端。

## 七、伪造 user 进入页面后的"露馅 / 无感"推演

前置操作：在浏览器控制台执行（无需任何真实账号）——

```js
localStorage.setItem('user', JSON.stringify({ id: 999, username: '张三', role: 'ADMIN' }));
```

然后访问 `/products`。路由守卫 [router/index.js:44-45](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-thor/frontend/src/router/index.js#L43-L52) 只判断 `localStorage` 有无 `user` 键，键存在即放行，**直接进入系统**。

### 7.1 完全无感（察觉不到是伪造）的地方

- **能进所有受保护页面**：`/products`、`/categories`、`/suppliers` 全部放行，与真实登录无差别。
- **顶栏用户名显示为伪造值**：`username` 是 `computed`，直接 `JSON.parse(localStorage['user']).username`（[App.vue:76-82](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-thor/frontend/src/App.vue#L76-L82)）。所以顶栏会**如实显示你编的名字"张三"**——它从不向后端核对，因此不会露馅，反而看起来"登录正常"。若把 username 编成 "admin"，页面上就是 "admin"。
- **各业务页面数据照常加载**：因为列表数据走的是无鉴权的 GET 接口，与登录态无关，页面能正常拉到商品/分类/供应商，增删改也都能成功（后端不校验）。
- **role 字段无处校验**：前后端都没有基于 `role` 做任何分支，因此伪造成 ADMIN 或乱填 role 都无差别、无提示。
- **刷新后依然"在线"**：状态在 `localStorage`，刷新/重开浏览器仍在，和真实登录表现一致。

### 7.2 会"露馅"或行为异常的地方

- **点"退出登录"表现完全正常，不会露馅**：`handleLogout` 只是 `localStorage.removeItem('user')` 再跳 `/login`（[App.vue:84-87](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-thor/frontend/src/App.vue#L84-L87)）。它不调后端、不清任何服务端会话（本来也没有）。所以伪造用户点退出，和真实用户点退出**行为一模一样**——键被删掉、回到登录页。这一步同样发现不了伪造。
- **真正会暴露的只有"user 内容本身不合法"时**：
  - 若塞入的值不是合法 JSON（例如 `localStorage.setItem('user','abc')`），路由守卫仍因"键存在"而放行，但 [App.vue:76-82](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-thor/frontend/src/App.vue#L76-L82) 里的 `JSON.parse` 会抛异常，导致顶栏 `username` 计算失败/渲染报错——**这是唯一肉眼可见的破绽**，但只要按 7 节开头那样塞入合法 JSON 就完全不会出现。
  - 若塞入合法 JSON 但没有 `username` 字段（如 `{"role":"ADMIN"}`），`JSON.parse(...).username` 为 `undefined`，顶栏用户名处会显示为空白——属于轻微异常，但不影响使用，也很容易通过补上 username 规避。

### 7.3 推演结论

伪造 user 后，**除非故意塞入非法 JSON，否则页面上没有任何地方会露馅**：能进所有页面、顶栏显示你编的名字、数据照常读写、退出登录行为也正常。这正印证了第一轮的结论——前端登录态是"自证式"的，它拿 `localStorage` 里的内容自己显示、自己判断，从不与后端交叉验证，所以伪造几乎无法被前端察觉。

## 八、与前轮结论的对照

本轮逐接口核对与页面推演，**未发现与第一轮结论相矛盾之处**，方向完全一致（前端拦截是摆设、后端全开放）。以下是相对第一轮的**细化与新增点**（补充，非推翻）：

1. **接口数量精确化**：第一轮定性说"商品/分类/供应商接口无鉴权"。本轮完整清点为 **5 个控制器共 18 个接口全部无鉴权**，并新增覆盖了第一轮未逐项展开的 **采购单 `/api/orders`（4 个接口）**——它暴露了采购价、供货关系等经营数据，且前端没有对应页面。
2. **注册接口危害升级**：第一轮提到"任何人可自助注册"。本轮进一步确认注册接口的 `role` 由请求体直接决定且不校验，**可直接注册出 ADMIN 账号**（虽然当前 role 在系统里没被用到，但一旦将来加权限就是隐患）。
3. **"退出登录会露馅"的澄清**：第一轮未展开退出行为。本轮推演明确——**点退出登录不会露馅**，它和真实用户表现完全一致（仅删 `localStorage` 键并跳转），因为登出也不涉及后端。这是对第一轮的补充说明，不构成冲突。
4. **唯一破绽的边界**：新增指出伪造场景下"唯一可见破绽"仅在于塞入**非法 JSON / 缺 username 字段**时顶栏渲染异常；只要构造合法 user 对象即完全无感。这一点第一轮未涉及，属细节补充。
5. **一处需要留意的健壮性问题（非鉴权，但本轮核查时发现）**：`updateXxx` 系列对不存在的 id 会 `orElseThrow(RuntimeException)` → 返回 500；而 `deleteById` 对不存在 id 静默成功。这属于错误处理不一致，与鉴权无关，仅在此登记，不改变主结论。

---
---

# 整改方案（第三轮 · 只出方案，不动代码）

> 本节承接前两轮结论，给出把鉴权"真正补上"的落地方案。前面第一~八节内容一律未改动。方案落到具体文件与接口，说明"动什么、为什么"，并给出路线取舍、改动先后，以及附带问题的处理建议。

## 九、路线选择：Token 还是 Session

先定路线，因为它决定后面所有文件怎么改。

| 维度 | Session（服务端会话 + Cookie） | Token（JWT，无状态） |
|------|-------------------------------|----------------------|
| 服务端状态 | 需要存储会话（内存/Redis），有状态 | 无状态，签名自校验 |
| 前端携带方式 | 浏览器自动带 Cookie | 需手动放 `Authorization` 头 |
| 与当前前端契合度 | 前端要改成不手动管 token，但要处理 CSRF | 与现有"登录后存点东西、每次请求带上"的思路几乎一致 |
| CSRF | 需要额外防护（因 Cookie 自动携带） | 放在 Header 里，天然规避 CSRF |
| 注销/失效 | 服务端删会话即时生效 | 需黑名单或短过期时间才能提前失效 |
| 部署复杂度 | 多实例要共享会话存储（引 Redis） | 无需共享存储，水平扩展简单 |

**取舍结论：这套系统选 Token（JWT）。** 理由：

1. **改造面最贴合现状**：现在前端已经是"登录成功→在 `localStorage` 存东西→（本该）每次请求带上"的形态，只是"带上"这步缺失。换成 JWT，只需把 localStorage 里存的从"用户信息"换成"token"，再补一个请求拦截器带 `Authorization` 头即可，前端改动最小、心智模型不变。
2. **后端目前是纯 REST + 无状态**，本身没有会话基础设施，选 Session 反而要额外引入会话存储（单机可用内存，但一旦多实例/容器重启就丢），成本更高。
3. **CORS 已经全开**（`origins="*"`）：若走 Cookie 方案，`*` 与携带凭证的 Cookie 不兼容（浏览器会拒绝 `Access-Control-Allow-Origin: *` + `credentials`），还得先收紧 CORS 才能用；而 JWT 放 Header 不受这个限制，迁移期更平滑。

> 但要明确 JWT 的代价并配套：**JWT 无法即时注销**。因此约定短有效期（如 access token 30~60 分钟），登出只在前端删 token；若业务要求"服务端强制踢下线"，再补 refresh token + 黑名单，但初版可不做。**切记 token 不放 `localStorage` 更稳妥（防 XSS 窃取），但因当前前端已重度依赖 localStorage 且无 XSS 加固，初版沿用 localStorage 存 token 可接受，作为后续优化项登记。**

## 十、后端要动什么（落到文件 / 接口）

### 10.1 引入 Spring Security + JWT 基础设施（新增依赖与配置）

- **`backend/pom.xml`**：新增 `spring-boot-starter-security` 与 JWT 库（如 `io.jsonwebtoken:jjwt`）。这是前提——前两轮已确认当前 **没有** security 依赖，所以全接口默认放行。
- **新增 `config/SecurityConfig.java`**：定义 `SecurityFilterChain`。核心规则：
  - `permitAll`：`POST /api/auth/login`、`POST /api/auth/register`（登录/注册必须匿名可达，否则没人能拿到 token）。
  - `authenticated`：**其余全部** `/api/products/**`、`/api/categories/**`、`/api/suppliers/**`、`/api/orders/**`。
  - `csrf().disable()`（因走 Header token，非 Cookie，无 CSRF 面）+ `sessionCreationPolicy(STATELESS)`（无状态，不建 HttpSession）。
  - 收紧 CORS（见第十二节），不再用通配 `*`。
- **新增 `config/JwtUtil.java`**：签发/校验 token（签名密钥从配置读，写入 username、role、过期时间）。
- **新增 `config/JwtAuthenticationFilter.java`**（继承 `OncePerRequestFilter`）：从 `Authorization: Bearer <token>` 解析并校验，塞入 `SecurityContext`。注册到 filter chain。
- **`application.properties`**：新增 `jwt.secret`、`jwt.expiration` 配置项（密钥不要硬编码进代码/仓库）。

> **为什么这么动**：有了这条 filter chain，鉴权就从"前端自觉"变成"后端强制"。这是整改的地基——前两轮的根因就是"后端根本不校验"，这一步直接堵住 18 个接口里的 16 个（除 login/register）。

### 10.2 改造登录接口 —— [AuthController.java](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-thor/backend/src/main/java/com/supermarket/controller/AuthController.java)

- **`login`（第 35-51 行）**：校验通过后，不再只回 `{id,username,role}`，而是用 `JwtUtil` 签发 token，返回 `{token, username, role}`。这是"后端真正下发凭证"的那一刻——补上前两轮指出的"登录成功却不发凭证"的缺口。
- **`register`（第 20-33 行）**：见第十二节，需去掉"role 由请求体决定"的问题（强制 `role=USER`），并把明文密码改为 `passwordEncoder.encode(...)`。
- **登录比对逻辑（第 39 行）**：`user.getPassword().equals(...)` 明文比对改为 `passwordEncoder.matches(...)`（配合第十二节的密码加密）。

### 10.3 各业务 Controller

- 商品/分类/供应商/采购单 4 个 Controller **本身不必逐个加注解**——只要 10.1 的 `SecurityConfig` 用路径规则统一 `authenticated`，它们就整体受保护。这样避免在每个方法上撒 `@PreAuthorize`、减少遗漏。
- **可选（第二阶段，基于 role 做写权限区分）**：如果要区分"USER 只读、ADMIN 可增删改"，则对写操作方法加 `@PreAuthorize("hasRole('ADMIN')")`，涉及：
  - `ProductController` 的 POST/PUT/DELETE（第 32-55 行）
  - `CategoryController` 的 POST/DELETE（第 24-33 行）
  - `SupplierController` 的 POST/PUT/DELETE（第 24-46 行）
  - `PurchaseOrderController` 的 POST/PUT/DELETE（第 27-51 行）
  - 这一步同时让登录返回的 `role` 字段"从死数据变成真正生效的权限"——前两轮指出 role 目前完全没被使用。

## 十一、前端要动什么（落到文件）

### 11.1 请求层补拦截器 —— [api/request.js](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-thor/frontend/src/api/request.js)

前两轮的关键断点就在这里——**当前只有响应拦截器，没有请求拦截器，身份信息从不进入请求**。需要补两段：

- **新增请求拦截器**：从 `localStorage` 取 token，若存在则加 `config.headers.Authorization = 'Bearer ' + token`。这是让"登录态真正传到后端"的那根线。
- **扩展响应拦截器（现第 10-17 行）**：捕获 `401/403`，清除本地登录态并跳转 `/login`（处理 token 过期/无效）。目前它只弹错误提示，不处理鉴权失败。

### 11.2 登录页 —— [Login.vue](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-thor/frontend/src/views/Login.vue)

- **`handleLogin`（第 82-94 行）**：后端改为返回 `{token, username, role}` 后，这里要 `localStorage.setItem('token', data.token)`，并把用于展示的 `username/role` 单独存（如 `localStorage.setItem('user', ...)` 只留展示字段）。核心是"存的是凭证 token，而非可随意伪造的身份 JSON"。

### 11.3 路由守卫 —— [router/index.js](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-thor/frontend/src/router/index.js#L43-L52)

- **`beforeEach`（第 43-52 行）**：判据从"`localStorage` 有没有 `user`"改为"有没有 `token`"。
- **必须认清其定位**：即便改判 token，前端守卫**仍然只是 UX 层**（决定跳不跳登录页），**真正的安全边界永远在后端**。前两轮已证明前端判断可被控制台绕过，所以守卫改动是锦上添花，不能替代 10.1。这点要在方案里讲透，避免又退回"前端拦一下就以为安全了"。

### 11.4 登出 —— [App.vue](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-thor/frontend/src/App.vue#L84-L87)

- **`handleLogout`（第 84-87 行）**、**`username` computed（第 76-82 行）**：把读写的键从 `user` 同步到新的存储约定（清 token + 展示信息）。行为上仍是"删本地 + 跳登录页"，因 JWT 无状态、无需通知后端。

## 十二、附带问题：要不要一起处理

**建议一起处理**，因为它们与鉴权同源（都是"后端没设防"），且改动点正好落在同一批文件，分开做反而重复劳动。

1. **明文密码（必须一起改）** —— [AuthController.java:39](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-thor/backend/src/main/java/com/supermarket/controller/AuthController.java#L39)、[DataInitializer.java:39-45](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-thor/backend/src/main/java/com/supermarket/config/DataInitializer.java#L36-L48)：
   - 引入 `BCryptPasswordEncoder`（随 Spring Security 自带）。register 时 `encode`，login 时 `matches`。
   - `DataInitializer` 里初始化的 `admin/user` 密码也要改成 `encode` 后写入，否则加密登录逻辑对不上明文库数据而全部登录失败。
   - 理由：一旦库泄露，明文密码直接暴露；且加密与 10.2 的登录改造是同一处代码，顺手一起做。

2. **注册可自带 role（必须一起改）** —— [AuthController.java:20-33](file:///d:/charles/program/ai/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731_00007/gsb0731_00007-thor/backend/src/main/java/com/supermarket/controller/AuthController.java#L20-L33)：
   - 服务端**强制** `user.setRole("USER")`，忽略请求体传入的 role。
   - 理由：前两轮已指出可直接注册 ADMIN。一旦第 10.3 的 role 权限区分上线，这个漏洞就等于"任何人自助获得管理员"，必须在 role 生效前先堵。

3. **CORS 全开（建议一起改，且是 Token 路线的前置项）** —— 各 Controller 上的 `@CrossOrigin(origins = "*")`（5 个控制器类注解）：
   - 移除类级 `@CrossOrigin(origins="*")`，改为在 `SecurityConfig` 统一配置白名单来源（仅前端部署域名）。
   - 理由：`*` 让任意第三方站点 JS 都能跨域打接口，放大了无鉴权的危害；集中到一处配置也便于维护。属"应一起做"的收敛项。

## 十三、改动先后（按前两轮的 18 接口清单排优先级）

排序原则：**先堵"匿名可写/可提权"的高危口，再堵读接口，最后做体验与分级权限。** 依据前两轮清单，高危集中在"能改能删"和"能自助拿管理员"。

- **P0 · 必须最先堵（提权 + 破坏性写 + 凭证地基）**
  1. 建立后端鉴权地基：10.1 的 `SecurityConfig` + JWT filter（一步到位保护 16 个业务接口）。**这是所有后续的前提。**
  2. 注册强制 `role=USER`（接口 #1）——堵住"自助拿 ADMIN"，否则鉴权上线也能被绕。
  3. 登录下发 token + BCrypt（接口 #2 / 10.2 / 12.1）——否则前端拿不到凭证，整条链路接不通。
  4. 最高破坏性写接口：商品 PUT/DELETE(#6/#7)、供应商 PUT/DELETE(#13/#14)、采购单 PUT/DELETE(#17/#18)、分类 DELETE(#10)。这些在 10.1 生效后即被覆盖，无需逐个改，但要**优先验证**它们匿名调用确实已返回 401。

- **P1 · 紧随其后（防匿名造数据 + 数据/PII 泄露）**
  5. 各 POST 新建接口（#5/#9/#12/#16）——同样由 10.1 覆盖，验证匿名不可写。
  6. 读接口：供应商 GET(#11，含 PII)、采购单 GET(#15，经营数据)优先，其次商品/分类 GET(#3/#4/#8)。

- **P2 · 前端配合与体验（不改变安全边界，但让系统可用）**
  7. 前端 request.js 请求拦截器 + 401 处理（11.1）、Login.vue 存 token（11.2）、路由守卫改判 token（11.3）、App.vue 登出/展示同步（11.4）。
  8. CORS 白名单收敛（12.3）。

- **P3 · 增强（可迭代）**
  9. 基于 role 的写权限分级（10.3 可选项，`@PreAuthorize`）。
  10. token 存储加固（HttpOnly Cookie 或防 XSS）、refresh token/黑名单、登录限流（应对接口 #2 撞库）、update 对不存在 id 的错误处理统一（第八节第 5 点登记的健壮性问题）。

**一句话**：P0 的 `SecurityConfig` 一旦落地，前两轮清单里 16 个匿名可写/可读接口同时被关；再配合"注册禁 role + 登录发 token + 密码加密"，就把"提权"和"接不通"两个致命点补齐。前端四处改动属 P2，是让系统恢复可用、体验闭环，但**始终记住安全边界在后端，前端守卫只是 UX**。
